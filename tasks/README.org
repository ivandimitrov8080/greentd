#+TITLE: greentd — full implementation task list
#+SUBTITLE: A faithful Warcraft III "Green TD" clone on Bevy 0.19 + lightyear 0.29
#+TODO: TODO NEXT IN-PROGRESS WAITING BLOCKED | DONE CANCELLED
#+TAGS: FOUNDATION(f) SIM(s) MAP(m) WAVES(w) TOWERS(t) ECON(e) BUILDER(b) LOBBY(l) NET(n) UI(u) CONTENT(c) TEST(x) RELEASE(r)
#+FILETAGS: :greentd:
#+STARTUP: overview indent

* How to work in this directory
  :PROPERTIES:
  :ID:       meta-howto
  :END:

  This directory is the single source of truth for *what is left to build*. It
  is a plan written against the code that already exists in =src/=, not against
  an imagined greenfield project. Every task names the file it touches.

  Workflow:

  1. Read the *Milestones* section to find the current milestone.
  2. Open the file for the subsystem, find the highest-priority =TODO= that is
     not =BLOCKED=, and move it to =NEXT=.
  3. On completing a task, tick its acceptance checkboxes, set it to =DONE=,
     and update =:STATUS:= in the file header if the subsystem is finished.

  Run =C-c a= style agenda?  No. This project has no org agenda config; entries
  are meant to be read as a checklist. Use =org-todo= and =org-cycle= normally.

** Task format (mandatory)
   :PROPERTIES:
   :ID:       meta-format
   :END:

   Every task heading is written exactly like this:

   #+BEGIN_SRC org
   ** TODO [#A] Imperative title, no trailing period          :@sim:
      :PROPERTIES:
      :ID:         widget-004
      :EFFORT:     1d
      :BLOCKED-BY: widget-001
      :END:
      Why this exists, in one or two sentences. Cite the WC3 behaviour being
      reproduced, or the defect being fixed, by ID (=D12=).

      - Files: =src/sim.rs=, =src/game.rs=
      - Acceptance:
        - [ ] Concrete, testable statement.
        - [ ] Another one.
   #+END_SRC

   Rules:

   - =ID= is unique across the whole directory, =subsystem-NNN=, lowercase.
   - =EFFORT= is a rough sizing: =2h=, =1d=, =1w=. It is a schedule input, not
     a promise.
   - =BLOCKED-BY= lists IDs that must be =DONE= first. =BLOCKS= is the inverse
     and is optional (it is derivable, so do not bother keeping it in sync).
   - Priority =A= means correctness, faithfulness or blocking; =B= means
     meaningful gameplay; =C= means polish. Anything that can lose a player's
     match data or let a client cheat is =A= by definition.
   - Acceptance criteria must be checkable without reading the diff. "Feels
     better" is not an acceptance criterion.
   - Do not use =#+TODO:= inside a task file to invent new keywords. The four
     active states are =TODO NEXT IN-PROGRESS WAITING BLOCKED=.

** Effort vocabulary
   :PROPERTIES:
   :ID:       meta-effort
   :END:

   | Level | Meaning                                                |
   |-------+--------------------------------------------------------|
   |     2h | one sitting, one file, no design decisions left open  |
   |     1d | one file plus its tests, one design decision           |
   |     1w | new subsystem, multiple files, protocol or data-model change |
   |     1m | multi-week epic; should be split before starting       |

* Status snapshot — what exists today
  :PROPERTIES:
  :ID:       status
  :END:

  =cargo run= (host), =cargo run -- server=, =cargo run -- client [port]= all
  work. The project builds; =Cargo.lock= pins bevy 0.19.1 and lightyear 0.29.0.
  What is implemented is a *vertical slice*: one map, one wave loop, three
  towers, one lose condition, replicated state, and a text HUD.

  *M0's exit criterion is met*, and the milestone is not formally =DONE=, which
  is a distinction worth keeping. The criterion is "no client can affect another
  player's towers; the sim has tests; the balance numbers live in one table;
  server runs headless" -- all four hold and the last one is now held by CI. But
  the rule above says a milestone ends when every =[#A]= task in it is =DONE=,
  and two are not: =found-002= (one acceptance box needs a second machine to join
  a host on another box) and =test-001= (the harness works; the task is open
  because its siblings keep arriving). =audit-012= is =WAITING= on a human
  sign-off rather than on work. Those three are listed under *Progress against
  M0* and nowhere else, so read the milestone as "unblocked" rather than
  "stamped".

** Implemented
   :PROPERTIES:
   :ID:       status-implemented
   :END:

   - One crate with a library root and a thin binary: the three run modes share
     one =App= builder in =src/main.rs=, and =src/lib.rs= owns the modules so
     =tests/= can link them.
   - =src/sim/=: server-only authoritative state machine, split into `mod.rs`, `waves.rs`, `combat.rs`, `economy.rs` and `rng.rs` (found-005). =HashMap= of
     =CreepId -> Creep=, =IVec2 -> Tower=, =PlayerKey -> Player=. Plain
     =step(dt) -> Vec<SimEvent>=, no networking types, unit-tested headless by
     [[file:12-testing.org][12-testing.org]] =test-001= (see =tests/sim_smoke.rs=).
   - Creep model: distance-along-path, =hp/max_hp=, =base_speed=,
     =slow_mult=+ =slow_timer=, =bounty=, =last_hit_by=.
   - Wave loop: infinite, 20 s between waves, first wave at 3 s, both from =assets/balance/match.ron=. Count is
     =(4 + wave/2) * connected_players=; =hp = 45 * (1 + 0.28*(w-1))=;
     =speed = 55 + 2*(w-1)=; =bounty = 6 + w/2=. Creeps are spread evenly
     around the whole loop.
   - Tower model: =kind= 0/1/2 = Basic/Cannon/Frost, from =assets/balance/towers.ron=;
     unbounded =level=; per-shot tier multipliers on damage, range and rate, with
     the growth fractions themselves read from the table (=damage_per_level=,
     =range_per_level=, =slow_duration=) as of =audit-011=.
     Targeting picks the creep furthest along the path inside range. Cannon
     splashes, Frost slows.
   - =try_build/ try_upgrade/ try_sell/ call_wave= are the only mutators, each
     returning =Result<(), Reject>=.
   - Lose condition: =creeps.len() > =match_rules.overrun_cap= (=150= in
     =assets/balance/match.ron=)= ends the match permanently.
   - =src/map.rs=: 32x24 grid, =TILE = 32.0=, one closed ring path through four
     corner waypoints; buildability is "not within =0.95 * TILE= of the path".
   - =src/net/protocol.rs=: =ClientCmd= (C2S) and =ServerNotice= (S2C) messages (in =src/net/messages.rs=),
     one bidirectional reliable channel, six replicated components.
   - =src/net/server.rs=: player identity is the client's own =PlayerId=, sent in
     a versioned =Handshake= and resolved to a stable =PlayerKey= by =net-002=
     (it was the UDP peer address until then), one private =PlayerView= entity
     per player, one global =MatchView= entity, sim state mirrored into
     replicated entities every =Update= with change-diffing.
   - =src/net/client.rs= and =src/ui/hud.rs=: no prediction, no interpolation. Text HUD, keys 1/2/3,
     LMB build, U upgrade, RMB sell, Space calls the next wave.
   - =src/ui/visuals.rs=: orthographic camera with =AutoMin= scaling, 768 tile
     sprites, creep sprite darkened by HP fraction, tower sprite coloured by
     kind and scaled by level. Sprites are attached by =On<Add, ..>= observers.

** NOT implemented (the bulk of the game)
   :PROPERTIES:
   :ID:       status-missing
   :END:

   No armour or damage types; no air units; no creep variety whatsoever; no
   finite wave table; no difficulty; no game modes; no lives/leak model; no
   income; no bounty scaling table; no gambling; no lumber; no food; no builder
   hero; no skills; no crits; no aura towers; no traps; no spell tower; no
   research tower; no tower tiers or branching (3 towers × N levels is not the
   same design — the original branches at tier 3 into different towers); no
   projectiles; no audio; no lobby or setup phase; no kick; no restart; no
   reconnect; no spectators; no multiboard; no tower tooltips or build panel;
   no minimap; no camera controls; no unit-health bars; no floating damage
   text.

   (That list ended "no CI" until it was checked against the tree. CI landed in
   =found-007= and has been green since; the sentence was simply never
   corrected, which is the failure mode a "NOT implemented" list has by
   construction -- nothing in =src/= contradicts it, so nothing catches it.
   =audit-011='s task body had the same shape: it named the literals it was
   about *while offering not to remove them*.)

** Progress against M0
   :PROPERTIES:
   :ID:       progress-m0
   :END:

   Updated as M0 lands. The audit file is the authority on each task; this is
   only a reading aid.

   | Task        | State       | What landed |
   |-------------+-------------+-------------|
   | =found-001= | DONE        | =engine_plugins= gives a dedicated server =MinimalPlugins= instead of a window; =VisualsPlugin= is client-only (D15). Also fixed host mode, which could not start at all, and added the readiness line CI waits for. |
   | =found-002= | IN-PROGRESS | =src/config.rs=; every address, the tick rate, the map, the balance directory, the log filter and the two command budgets are resolved from the CLI, the environment and a config file, in that order. =tests/config.rs= holds the precedence matrix. Owed: the remote-host join and the exit code, both of which need a process. |
   | =found-003= | DONE        | =src/logging.rs=: four stable targets, the level policy, and a per-match log file named after the start time. This is the crate's only wall clock. |
   | =found-004= | DONE        | =src/data/balance.rs= and =assets/balance/*.ron=; every tuned number is data, validated and hashed. D18 is =DONE= as of =audit-011=, which removed the last per-tier and refund literals from =src/sim/=. |
   | =found-005= | DONE        | The flat crate is now =data/=, =sim/=, =net/= and =ui/=, with the reason for each placement written down in =src/main.rs=. |
   | =found-006= | DONE        | One =StartupError= for the config and the balance loader; the last =unwrap()= in =src/= (=Path::sample=) is NaN-safe. |
   | =found-007= | DONE        | =.github/workflows/ci.yml= (=check=, =smoke=, =release=), =rust-toolchain.toml=, =ci/smoke-server.sh= and =ci/check-balance-refusal.sh=. Unblocked by dealing with the 16 standing clippy warnings, with =-D warnings= as the gate from here on. |
   | =found-008= | DONE        | =Cargo.toml= carries the resolved-version table and a justification per lightyear feature; =rust-toolchain.toml= pins 1.97.1; the upgrade procedure is the =decide-upgrade-path= decision below. The MSRV is lightyear's 1.95, not the edition's 1.85. |
   | =found-009= | DONE        | =sim::Rng=, seeded from =match_rules.seed=; the wave's spawn jitter is its first consumer. Same seed, same match -- proven, not asserted. |
   | =found-010= | DONE        | =data::components::LocalPlayer=, marked by the server on a host and by the client on a remote peer; =MatchView.players= replaces counting the views the HUD happens to hold. Closes D30 and =audit-028=. |
   | =audit-001= | DONE        | =Reject= gains =NotOwner=, =RateLimited= and =NotInMatch=. |
   | =audit-002= | DONE        | Ownership enforced in =try_upgrade=/=try_sell=; refunds go to the owner (D1, D2). |
   | =audit-003= | DONE        | A private =PlayerView= is created for every peer and put on the wire only for a peer that does not already hold it, so host mode no longer broadcasts every view and the HUD cannot read the wrong player (D3). |
   | =audit-004= | DONE        | =Phase= is published and the mirror keeps running after defeat (D4). |
   | =audit-005= | DONE        | =WaveStarted= is broadcast and observed at the client (`notice: WaveStarted(1)` in a live run), so the wave notice is live code (D5). |
   | =audit-006= | DONE        | =call_wave(key)=, a per-player =wave_call_cooldown= from the balance table, run down in =step=, refused with a reason (D6). |
   | =audit-007= | DONE        | =src/ratelimit.rs= and =Net::buckets=; a peer spends =Config='s burst and rate, and the rest of the frame's commands are dropped, not deferred (D7). |
   | =audit-008= | DONE        | No handler mints a player; =handle_cmds= drops unregistered peers (D8). |
   | =audit-010= | DONE        | =tests/sim_smoke.rs= + =tests/balance.rs=; zero tests is no longer true (D17). |
   | =audit-017= | DONE        | The audit-side check for D15, now permanent: the smoke job starts the real server with the display environment unset. |
   | =audit-029= | DONE        | *D31, found while building CI.* Host mode ran a =RawClient= over UDP beside a =RawServer=, which lightyear calls =MixedClientServer= and which killed the process about three seconds into wave 1. A host is now a real lightyear host client: in-process messages, no per-peer copy of anything. |
  | =audit-030= | DONE        | *D32, found by running a server and a client side by side.* =on_client_connected= inserted a default =MessageManager= over the engine-managed one, wiping the sender registration; every notice to a remote client was dropped inside the process. |
  | =audit-031= | TODO        | *D33.* A windowed peer stalls after one to two seconds here, dropping the packets that make =audit-005= and =audit-003= verifiable end to end. =[#A]= because an unexplained main-loop stall is either an environment fact worth recording or a deadlock. |
   | =test-001=  | IN-PROGRESS | Same harness. Its last box was blocked on =found-009= for the seeded RNG; =found-009= has landed, so the box is now true and unticked by choice -- see the task, which records both readings and says the choice is a milestone owner's to make. |
   | =audit-011= | DONE        | The last literals left =src/sim/=. =towers.ron= gained =damage_per_level=, =range_per_level= and =slow_duration=; =match.ron= gained =sell_refund_percent=; both are validated, hashed into the handshake, and tested (=the_last_sim_literals_are_data_now=, =the_tier_scaling_is_read_from_the_table=, =the_slow_duration_is_read_from_the_table=). D18 is =DONE=; the flat tier *model* is still =towers-002='s. |
   | =audit-012= | WAITING     | Blocked on =decide-default-lineage=, which is a sign-off, not a task. |
   | =audit-009= | DONE        | Closed by =net-002=: identity is the client's =PlayerId=, not the socket address. The transcript shows a client rejoining from a new port (5101 to 5102) re-entering the same =PlayerKey(42)= and the same =PlayerView= entity. D9 is =DONE=. |
   | =audit-019= | DONE        | Closed by =net-001=: the refusal is the handshake's other outcome, so the three claims this task makes are the three =net-001= verified. D22 is =DONE=. |

  Everything above builds, passes =cargo test= (82 tests: 3 + 8 + 25 + 7 + 7 + 8 +
  24, i.e. the crate's own unit tests plus =tests/balance.rs=, =tests/config.rs=,
  =tests/identity.rs=, =tests/protocol.rs=, =tests/ratelimit.rs= and
  =tests/sim_smoke.rs=) and =cargo clippy --all-targets -- -D warnings=, and was
  walked by hand in a running =host=, =server= and =client=. CI's three jobs'
  commands were run locally in full, including the headless smoke run and the
  balance-refusal check.

  The count is a running total, not a target: it moved 64 -> 71 -> 79 -> 82 as
  =net-001=, =net-002= and =audit-011= landed, and the arithmetic in this line
  is only worth keeping in step with =cargo test='s own output.

  *M0's exit criterion holds; the milestone is unblocked rather than stamped.*
  The four things the criterion names are true -- no client can touch another
  player's towers, the sim has tests, the balance numbers are one table, and the
  server runs headless under CI -- but three tasks keep the milestone from being
  *stamped*, and they are open for three different reasons, which is worth
  separating rather than lumping:

  - =found-002= is =IN-PROGRESS= for a reason no amount of work on this machine
    resolves: its last two boxes need a second machine to join a non-local host,
    and a process to check an exit code.
  - =test-001= is =IN-PROGRESS= by *choice*, not by blocker. Its last box was
    held on =found-009='s seeded RNG, which has landed, so the box is now true and
    unticked; the task records both readings and calls it a milestone owner's
    decision. This is the one of the three that is not waiting on anything.
  - =audit-012= is =WAITING= on =decide-default-lineage=, which is a sign-off
    rather than work, and it gates =sim-005= and so all of M1's lose conditions.

  What is left before M1, in the order it should be taken:

  - =audit-011= (D18) is *done*, and closing it was the last of the wire-format
    batch that =net-001= unblocked: =towers.ron= gained =damage_per_level=,
    =range_per_level= and =slow_duration=, =match.ron= gained
    =sell_refund_percent=, and =src/sim/= no longer holds a balance literal. The
    task's body had offered to leave the work to =towers-002= "if the tower
    rewrite slips"; the rewrite has not started, so that deferral was declined
    rather than inherited. See [[file:01-current-code-audit.org][01-current-code-audit.org]]
    for why, and for the rule it settles: a task is not discharged by a later
    task that happens to delete the same line.
  - =audit-031= (D33), the windowed-peer stall. It is now the *only* =[#A]=
    unclosed in the audit file, and it is the reason three boxes above are weaker
    than they read: it decides whether those verifications can become scripted or
    have to stay transcripts. It needs a display, so it is the one task here a
    headless session cannot finish -- though its first box ("ours or the
    presentation layer's?") is answerable without one.
  - =net-006='s last box, which is the network-design statement of what
    =audit-029= fixed mechanically, and still owns a test for "a host plus one
    remote client".
  - =found-002='s last box and =audit-020='s, which both need a second machine to
    join a non-local host. =audit-020= is the last member of the wire-format
    batch that is work rather than a component-set change.
  - =decide-default-lineage=, without which =sim-005= cannot start.

  =net-001= and =net-002= are both *done*, and closed =audit-019= and
  =audit-009= with them. Between them they cost two defects of the "a message
  that is never written" shape, both found by starting two processes by hand
  ([[file:09-networking.org][09-networking.org]]), and one transport fact the
  plan did not anticipate: raw UDP never reports a dead peer, which is why the
  identity rule is a logged takeover rather than a refusal.

  Two debts are left, and both are downstream of =audit-031= (D33), which is why
  that task is =[#A]= and why it is the next thing in this file after the rest of
  the netcode block:

  - Remote delivery is verified in one direction only. The server demonstrably
    *sends* every wave notice and every refusal, and a host demonstrably *shows*
    them; a remote client showing them was observed once and has not been
    reproduced, because the windowed client's frame loop stalls after one to two
    seconds here. Rather than present a transcript as a repeatable result, the
    boxes are left as they are and the measurement is on =audit-031=.
  - =audit-003='s box three has been verified structurally (the host's world holds
    two =PlayerView= entities and exactly one is marked) but not by making one
    player's gold diverge from another's, because this machine has no
    input-injection tool.

  The larger lesson of =audit-029= and =audit-030= is procedural and worth keeping
  where it will be read: *reading =src/= is what wrote this plan, and reading
  =src/= is what missed both defects.* One needed a process started by hand with
  the display taken away; the other needed two processes started by hand at once,
  with lightyear's own message trace turned on. Neither is covered by a test that
  exists today, and =test-008='s =App= harness is the only thing that would make
  them coverable.

* The single most important correctness question — two lineages
  :PROPERTIES:
  :ID:       lineage
  :END:

  "Green TD" is not one map. Two lineages share the name, and they have
  *different lose conditions*. The existing code picked one and its own comments
  assert it is "Green TD's real lose condition", which is only true of one
  lineage.

  | Lineage | Lineage A — "Green Circle TD" | Lineage B — "Green TD" / "Green TD PROs" |
  |---------+-------------------------------+------------------------------------------|
  | Author  | "hit" and many forks          | KaMiKaZePeNgUiN, later GenoHacker / Sencility |
  | Lose    | Too many creeps alive on the ring at once | Lives; a creep reaching the "Life Zone" leaks and costs lives |
  | Threshold | 600 / 700 / 800 / 1000 depending on version | Boss leak costs 10 lives; 0 lives = defeat |
  | Waves   | Endless                       | 36 waves (PROs) or 60 waves, then Endless |
  | Source  | hiveworkshop thread 350930    | hiveworkshop thread 249982, wc3maps 356685 |

  The current code implements Lineage A with =match_rules.overrun_cap = 150=
  (=assets/balance/match.ron=), which does not even match Lineage A's documented
  numbers (600–1000).

  **Decision:** support both, as data. The lose rule becomes a match setting
  (=lobby-003=) with two implementations (=sim-005=). Default for a fresh match
  should be Lineage B (lives), because "leak costs a life" is the mechanic most
  players remember and because it gives a longer, more forgiving ramp than a
  hard creep cap.

  This is not scope creep for its own sake: it is one enum, two match settings,
  and it prevents the project from being *wrong about the game it is cloning*.

* Milestones
  :PROPERTIES:
  :ID:       milestones
  :END:

  Milestones are ordered so that each one ends with a playable build. A
  milestone is done when every =[#A]= task inside it is =DONE= and the build
  runs end-to-end.

  =12-testing= is the one exception to the "each file belongs to one milestone"
  rule: M0's exit criterion is literally "the sim has tests", so
  [[file:12-testing.org][12-testing.org]] starts in M0 (=test-001= through =test-005=), continues
  through M1-M5, and finishes in M6 with the golden balance test. The other
  files each belong to exactly one milestone.

  | # | Name                        | File                                   | Exit criterion |
  |---+-----------------------------+----------------------------------------+----------------|
  | M0 | Correct and observable      | [[file:01-current-code-audit.org][01-current-code-audit]] + [[file:00-foundation.org][00-foundation]] + [[file:12-testing.org][12-testing]] (=test-001=..=test-005=) | No client can affect another player's towers; the sim has tests; the balance numbers live in one table; server runs headless |
  | M1 | Faithful core loop          | [[file:02-simulation.org][02-simulation]] + [[file:03-map.org][03-map]] + [[file:04-waves.org][04-waves]] | A full 60-wave match (the 36-wave short game is a prefix) with armour types, air, immune, boss and hero waves, and a real lose condition |
  | M2 | Tower system                | [[file:05-towers.org][05-towers]] | All 11 tower families with tiers, branching, target filters, auras, traps and projectiles |
  | M3 | Economy and builder         | [[file:06-economy.org][06-economy]] + [[file:07-builder.org][07-builder]] | Per-wave income, scaling bounty, gambling, food, and a levelling builder with skills |
  | M4 | Modes and lobby             | [[file:08-lobby.org][08-lobby]] | Setup phase, 5 difficulties + chaos, 7 game modes, tower disabling, kick, restart |
  | M5 | Netcode and UX              | [[file:09-networking.org][09-networking]] + [[file:10-ui.org][10-ui]] | Interpolated rendering, reconnect, spectators, multiboard, build panel, tooltips, camera controls |
  | M6 | Content and release         | [[file:11-content.org][11-content]] + [[file:12-testing.org][12-testing]] + [[file:13-release.org][13-release]] | Art and audio pass, balance pass, packaging, docs |

* Defect index (D-numbers)
  :PROPERTIES:
  :ID:       defects
  :END:

  These are the deficiencies the plan was written against, with a =State= column
  saying which are still live. The summaries describe each defect as it was
  found, so a closed row may quote an identifier that no longer exists in =src/=
  — that is the point of the row, not an error in it. Each is fixed by a task in
  [[file:01-current-code-audit.org][01-current-code-audit.org]] (except where noted) and referenced by ID from
  other files so a task can say "fixes D9" without restating it.

  =State= is =DONE= when the defect is closed, =part= when the task that owns it
  has started but the defect has not fully gone, and =open= otherwise. The audit
  file carries the same column; keep the two in step.

  =A= defects are exploitable or lose player state. =B= are fidelity gaps. =C=
  are polish. The full write-up of each is in the audit file.

  | D   | Sev | State | Summary |
  |-----+-----+-------+---------|
  | D1  | A   | DONE  | =try_upgrade= and =try_sell= never check that the caller owns the tower. Any client can drain or destroy any other player's board. |
  | D2  | A   | DONE  | =try_sell= refunds the *caller*, not the tower's owner. |
  | D3  | A   | DONE  | Host mode replicated every =PlayerView= to everyone (=NetworkTarget::All=), so the host held a duplicate of the whole match. Fixed by =audit-003= on top of =found-010=: a private view is now put on the wire only for a peer that does not already hold it, and the host reads the server's own view. |
  | D4  | A   | DONE  | =sync_match= early-returns when =sim.over=, freezing the HUD at the moment of defeat; =MatchView::phase= is never written. |
  | D5  | A   | DONE  | =ServerNotice::WaveStarted= is sent on the =GameOver= path (=audit-005=) and observed at the client. |
  | D6  | A   | DONE  | =CallWave= takes the caller, records a per-player cooldown from the balance table, and refuses with a reason (=audit-006=). |
  | D7  | A   | DONE  | A per-peer token bucket in =src/ratelimit.rs=; over-budget commands are dropped and the peer is told once per frame (=audit-007=). |
  | D8  | A   | DONE  | =players.entry(who).or_default()= mints a zero-gold player record for an unregistered key; diverges from =add_player='s starting gold. |
  | D9  | A   | DONE  | Identity is the UDP socket address, so reconnecting from a new port silently becomes a new player and loses gold and kills. Fixed by =net-002=: a client-generated =PlayerId= in the handshake, and a takeover rule, so a reconnect re-enters the same =PlayerKey= and the same =PlayerView=. |
  | D10 | B   | open  | A wave is spread evenly around the entire loop, so it has no front; and a creep can spawn already adjacent to the Life Zone. |
  | D11 | B   | open  | Creeps are entirely uniform — no armour type, no damage type, no air, no boss, no hero, no immune. |
  | D12 | C   | open  | =CreepVis= and =CreepHp= are distinct components, doubling replication updates per creep per tick. |
  | D13 | B   | open  | No projectile entities; towers damage invisibly on a cooldown. |
  | D14 | B   | open  | No client-side interpolation; creeps visibly step at 30 Hz. |
  | D15 | B   | DONE  | =VisualsPlugin= was added in =server= mode, so a dedicated server opened a window and allocated 768 sprites. Fixed by =found-001=: a server gets =MinimalPlugins= and no window. Held fixed by the =found-007= smoke job, which starts the real binary with the display environment unset (=audit-017=). |
  | D16 | C   | open  | Target acquisition is O(towers × creeps) per tick, ~115k distance tests at the current caps. |
  | D17 | A   | DONE  | Zero tests. The sim is written to be testable and never has been. |
  | D18 | A   | DONE  | Every balance number was a literal in =game.rs= or worse, inline in =sim.rs=. The tables became data in =found-004=; =audit-011= moved the remainder -- the per-tier damage and range growth, the slow duration and the sell refund -- into =towers.ron= and =match.ron=, so =src/sim/= holds no balance literal. |
  | D19 | A   | open  | The lose condition is the other lineage's lose condition and its constant (150) matches neither. See the lineage section above. |
  | D20 | B   | open  | =Sim::over= is terminal; there is no restart, rematch or -restart equivalent. |
  | D21 | B   | open  | =Net::announced_over= is never reset, so a restarted match would not announce again. |
  | D22 | B   | DONE  | No protocol version negotiation; a mismatched client desyncs silently instead of being rejected. Fixed by =net-001=: =ProtocolVersion= = schema revision + balance hash, exchanged in a =Handshake=, with a refusal that names the part that differed, before any entity exists. |
  | D23 | B   | open  | =spawn_client= hardcodes =127.0.0.1=; there is no way to join a remote host. |
  | D24 | B   | open  | A player joining mid-match starts at wave N with starting gold and no catch-up. |
  | D25 | B   | DONE  | =Reject= has no =NotOwner= and no =RateLimited= variant, so D1/D6 cannot report a reason. |
  | D26 | C   | open  | Grass/path colouring treats "inside the ring" and "outside the ring" identically; undocumented whether that is intended. |
  | D27 | B   | open  | No leaver handling: gold is not split, and towers are simply orphaned. |
  | D28 | C   | open  | =Path::distance_to= walks every segment; =is_buildable= is called per click on the server. Cheap now, wrong shape later. |
  | D29 | B   | open  | =VisualsPlugin='s =MapTile= is =#[allow(dead_code)]= and tiles are spawned as individual entities rather than a tiled render pass. |
  | D30 | C   | DONE  | No =LocalPlayer= marker; the client inferred "my" state from "the one =PlayerView= I happen to hold". Fixed by =found-010=, tracked as =audit-028=. |
  | D31 | A   | DONE  | Host mode ran a =RawClient= over a real UDP socket beside a =RawServer=, which lightyear rejects as =MixedClientServer= and which killed the process three seconds into wave 1. Fixed by =audit-029=, which makes a host a real lightyear host client: in-process messages, no per-peer copy of anything. |
  | D32 | A   | DONE  | =on_client_connected= inserted a fresh =MessageManager= over the one the engine manages, wiping the sender registration the message layer installs from an =on_add= hook. Every =ServerNotice= -- wave started, every refusal, game over -- was silently dropped on the way to a remote client. Fixed by =audit-030=. |
  | D33 | A   | open  | A windowed peer stops running its =Update= schedule after one to two seconds in a detached session: the socket buffer fills and the kernel drops 95% of the packets it is sent. Measured, not inferred; cause unknown. =audit-031=. |

* Cross-cutting decisions
  :PROPERTIES:
  :ID:       decisions
  :END:

** DONE Balance lives in data, not code                                  :@sim:
   :PROPERTIES:
   :ID:       decide-data-driven
   :EFFORT:   1d
   :END:

   All tower stats, creep stats, wave tables, ability numbers and drop rates
   move to RON assets under =assets/balance/=, loaded by both peers, hashed into
   the protocol version. Rationale: the original map's numbers were tuned by
   hand over years; this project needs to be able to do that without a
   recompile, and needs both peers to agree on the numbers or the "dumb client"
   model breaks (tooltips would lie).

   See [[file:00-foundation.org][00-foundation.org]] =found-004=.

** DONE Server is authoritative; the client never simulates                    :@net:
   :PROPERTIES:
   :ID:       decide-authority
   :END:

   Keep the existing model. A TD has no twitch input, so there is nothing to
   hide latency behind; prediction would add determinism obligations for no
   player-visible benefit. The client *does* get interpolation and a visual-only
   "ghost" of the tower it is about to place. This decision constrains
   [[file:09-networking.org][09-networking.org]] and [[file:10-ui.org][10-ui.org]] and should not be revisited
   without a concrete reason.

** DONE Pin the dependency set and write down the upgrade path              :@found:net:
   :PROPERTIES:
   :ID:       decide-upgrade-path
   :EFFORT:   2h
   :END:

   =bevy= and =lightyear= move together or not at all: lightyear 0.29 is written
   against bevy 0.19 and nothing else, so a bump of one is a rebuild of the
   other. Without a written procedure a version bump is an unbounded refactor,
   and without a pin the local and CI builds can disagree about which one they
   are building. This is the procedure; see [[file:00-foundation.org][00-foundation.org]]
   =found-008=.

   Pinned today (from =Cargo.lock=):

   | Crate     | Required | Resolved |
   |-----------+----------+----------|
   | bevy      | 0.19.1   | 0.19.1   |
   | lightyear | 0.29     | 0.29.0   |
   | ron       | 0.12     | 0.12.2   |
   | serde     | 1        | 1.0.229  |

   Edition =2024= needs rustc 1.85; lightyear 0.29.0 declares =rust-version =
   "1.95"=, which is the highest floor of any dependency and therefore the real
   one. =rust-toolchain.toml= pins =1.97.1=, the version the project is
   actually developed on, so the Nix devshell's =rustc= and CI's are the same
   binary.

   **Upgrade procedure**

   1. Read lightyear's release notes for the target version and find the bevy
      version they name. Bevy breaks API on every minor release and lightyear
      tracks it one-to-one, so the two are bumped in *one* commit. Raise
      =rust-toolchain.toml= in the same commit if the notes raise the MSRV.
   2. =cargo update -p bevy -p lightyear= -- never a bare =cargo update=, which
      floats every transitive crate at once and leaves no single suspect.
   3. =cargo build=, then re-check the plugin ordering in =src/main.rs=. The
      invariant is that =protocol::build_protocol(&mut app)= runs *after* the
      lightyear plugin groups and *before* any =Client= or =Server= entity
      exists: it fills a registry both halves read, and a peer created before it
      runs has no component table.
   4. Re-check the four feature groups in the =lightyear= entry in =Cargo.toml=:
      that each one still exists under that name, and that
      =default-features = false= is still what we want (the defaults enable
      =prediction= and =interpolation=, which =decide-authority= rules out).
   5. =cargo test=, then =cargo clippy --all-targets -- -D warnings=, then
      =cargo fmt --check=. The same three commands CI runs, in the same order.
   6. Run all three modes by hand: =cargo run= (host), =cargo run -- server=
      with =DISPLAY= unset, and =cargo run -- client= against it. A netcode
      change whose only symptom is a peer that never connects is exactly what
      this step catches.
   7. For any change to =src/net=/, re-run the remote pair with lightyear's
      message trace on and check that *both* directions appear:
      =RUST_LOG=info,greentd=debug,lightyear_debug::message=trace= on the server
      and the client at once, then grep for =message_send= and
      =message_receive_bytes= on each. This is step 6 with the instrument
      pointed at the wire, and it is here because =audit-030= (D32) was a
      two-word mistake that stopped every notice reaching a remote client while
      every test in =tests/= stayed green.
   8. Update the table above and the one in =Cargo.toml=, and say in the commit
      message which version was left behind.

** TODO Decide the default lineage for a fresh match                     :@sim:
   :PROPERTIES:
   :ID:       decide-default-lineage
   :EFFORT:   2h
   :END:

   Two lineages exist (see *The single most important correctness question*
   above). The recommendation is Lineage B (lives). This needs an explicit
   sign-off before =sim-005= is started, because it determines the whole
   difficulty curve and the map's Life Zone geometry.

* File index
  :PROPERTIES:
  :ID:       file-index
  :END:

  | File | Scope |
  |------+-------|
  | [[file:00-foundation.org][00-foundation.org]] | Build, config, headless mode, logging, data-driven balance loading, CI skeleton |
  | [[file:01-current-code-audit.org][01-current-code-audit.org]] | Fixes for D1–D30; the first thing to do |
  | [[file:02-simulation.org][02-simulation.org]] | Sim data model, armour/damage types, targeting, projectiles, RNG, lose/win rules |
  | [[file:03-map.org][03-map.org]] | Map data model, path shapes, lanes, Life Zone, build regions, map selection |
  | [[file:04-waves.org][04-waves.org]] | Creep catalogue, wave tables, wave kinds, difficulty scaling, endless |
  | [[file:05-towers.org][05-towers.org]] | The 11 tower families, tiers, branching, auras, traps, spells, research, crits |
  | [[file:06-economy.org][06-economy.org]] | Gold, income, bounty, sell values, gambling, lumber, food, leaver split |
  | [[file:07-builder.org][07-builder.org]] | Builder hero, XP, levels, skill points, emergency towers |
  | [[file:08-lobby.org][08-lobby.org]] | Setup phase, difficulty, chaos, game modes, tower disabling, kick, restart |
  | [[file:09-networking.org][09-networking.org]] | Protocol v2, authority hardening, reconnection, spectators, interpolation, dedicated server, matchmaking |
  | [[file:10-ui.org][10-ui.org]] | Multiboard, build panel, tooltips, camera, minimap, floating text, selection, notices |
  | [[file:11-content.org][11-content.org]] | Art, animation, VFX, audio, fonts, loading screen |
  | [[file:12-testing.org][12-testing.org]] | Unit/property tests, replay harness, bots, fuzz, benchmarks |
  | [[file:13-release.org][13-release.org]] | Packaging, platform targets, docs, telemetry, moderation |
