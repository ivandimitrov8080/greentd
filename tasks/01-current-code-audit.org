#+TITLE: greentd — current-code audit
#+SUBTITLE: One task per defect found in the code that already exists (D1–D34)
#+TODO: TODO NEXT IN-PROGRESS WAITING BLOCKED | DONE CANCELLED
#+TAGS: FOUNDATION(f) SIM(s) MAP(m) WAVES(w) TOWERS(t) ECON(e) BUILDER(b) LOBBY(l) NET(n) UI(u) CONTENT(c) TEST(x) RELEASE(r)
#+FILETAGS: :greentd:
#+STARTUP: overview indent

This is the first work to do. It touches =src/sim.rs=, =src/game.rs=,
=src/server.rs=, =src/client.rs=, =src/visuals.rs=, =src/map.rs=, =src/protocol.rs=
and the new =tests/= tree; each task names the D-number it closes and the exact
function or literal it changes. Every task that mutates =Sim= adds a test in the
same task: the headless harness exists as of =audit-010=, so there is no longer
an excuse for an untested mutation (D17).

* Defect triage

  Every D-number, the severity, whether it still exists, the files it lives in, a
  rough effort, and the task that fixes it. D15 and D30 are fixed in
  [[file:00-foundation.org][00-foundation.org]]; D18 was started there and finished here by =audit-011=;
  D34 is a defect in the =ci/= scripts, so its fix is =found-011= there and it has
  no audit task of its own.
  Where a fix lives in =00-foundation.org=, the audit task named beside it only
  tracks the defect, so this index stays complete.

  =State= is =DONE= when the defect is closed, =part= when the task that owns it
  has started but the defect has not fully gone, and =open= otherwise. It is
  derived from that task's heading in this file: do not close a row here without
  closing the task.

  | D   | Sev | File(s)                          | Effort | Fixed by                          | State |
  |-----+-----+----------------------------------+--------+-----------------------------------+-------|
  | D1  | A   | =src/sim.rs=                     | 1d     | =audit-002=                       | DONE  |
  | D2  | A   | =src/sim.rs=                     | 1d     | =audit-002=                       | DONE  |
  | D3  | A   | =src/server.rs=, =src/client.rs= | 1d     | =audit-003=                       | DONE  |
  | D4  | A   | =src/server.rs=, =src/game.rs=   | 2h     | =audit-004=                       | DONE  |
  | D5  | A   | =src/server.rs=, =src/game.rs=   | 2h     | =audit-005=                       | part  |
  | D6  | A   | =src/sim.rs=, =src/server.rs=    | 1d     | =audit-006=                       | part  |
  | D7  | A   | =src/server.rs=                  | 1d     | =audit-007=                       | part  |
  | D8  | A   | =src/sim.rs=                     | 2h     | =audit-008=                       | DONE  |
  | D9  | A   | =src/server.rs=, =src/sim.rs=    | 1w     | =audit-009= (via =net-002=)       | DONE  |
  | D10 | B   | =src/sim.rs=                     | 1d     | =audit-013=                       | open  |
  | D11 | B   | =src/sim.rs=, =src/game.rs=      | 1w     | =audit-014=                       | open  |
  | D12 | C   | =src/game.rs=, =src/protocol.rs= | 2h     | =audit-024=                       | open  |
  | D13 | B   | =src/sim.rs=, =src/server.rs=    | 1w     | =audit-015=                       | open  |
  | D14 | B   | =src/visuals.rs=                 | 1d     | =audit-016=                       | open  |
  | D15 | B   | =src/main.rs=                    | 1d     | =found-001= (tracked =audit-017=) | DONE  |
  | D16 | C   | =src/sim.rs=                     | 1w     | =audit-025=                       | open  |
  | D17 | A   | =src/sim.rs=                     | 1d     | =audit-010=                       | DONE  |
  | D18 | A   | =src/game.rs=, =src/sim.rs=      | 1w     | =found-004= (tracked =audit-011=) | DONE  |
  | D19 | A   | =src/sim.rs=                     | 1w     | =audit-012=                       | open  |
  | D20 | B   | =src/sim/mod.rs=, =src/net/server.rs= | 1d     | =audit-018=                       | DONE  |
  | D21 | B   | =src/net/server.rs=              | 2h     | =audit-018=                       | DONE  |
  | D22 | B   | =src/protocol.rs=                | 1w     | =net-001= (tracked =audit-019=) | DONE  |
  | D23 | B   | =src/client.rs=, =src/main.rs=   | 1d     | =audit-020=                       | open  |
  | D24 | B   | =src/sim.rs=                     | 1d     | =audit-021=                       | open  |
  | D25 | B   | =src/game.rs=                    | 2h     | =audit-001=                       | DONE  |
  | D26 | C   | =src/map.rs=, =src/visuals.rs=   | 2h     | =audit-026=                       | open  |
  | D27 | B   | =src/sim.rs=, =src/server.rs=    | 1d     | =audit-022=                       | open  |
  | D28 | C   | =src/map.rs=                     | 1w     | =audit-027=                       | open  |
  | D29 | B   | =src/visuals.rs=                 | 1d     | =audit-023=                       | open  |
  | D30 | C   | =src/client.rs=, =src/game.rs=   | 2h     | =found-010= (tracked =audit-028=) | DONE  |
  | D31 | A   | =src/main.rs=, =src/client.rs=   | 1d     | =audit-029=                       | DONE  |
  | D32 | A   | =src/net/server.rs=              | 2h     | =audit-030=                       | DONE  |
  | D33 | A   | =src/main.rs=                   | 1d     | =audit-031=                       | DONE  |
  | D34 | C   | =ci/=                            | 2h     | =found-011= (in =00-foundation=)  | open  |

* Severity A — exploitable or loses player state

  These come first. Every one either lets a client affect another player's board or
  match, or silently loses gold and kills.

** DONE [#A] Give Reject the NotOwner and RateLimited reasons                          :@sim:
   :PROPERTIES:
   :ID:         audit-001
   :EFFORT:     2h
   :END:

   D25: =Reject= in =src/game.rs= has =NotEnoughGold=, =Occupied=, =PathBlocked=,
   =OutOfBounds=, =BadKind=, =NoSuchTower= and =MatchOver=, so neither the ownership
   refusal (D1/D2, =audit-002=) nor the rate-limit refusal (D6/D7, =audit-006= and
   =audit-007=) has a reason to report. Add the two variants and their =text()=
   strings first, because the tasks below depend on them.

   - Files: =src/game.rs=, =src/server.rs=
   - Acceptance:
     - [x] =Reject= gains =NotOwner= and =RateLimited=, and =Reject::text()= returns a player-readable string for each.
     - [x] =Reject= still derives =Serialize/Deserialize/Clone/Copy/PartialEq/Eq= so it round-trips on the =ServerNotice::Err= path.
     - [x] A test round-trips every =Reject= variant through =ServerNotice::Err= and asserts each =text()= is non-empty, so no new variant is unhandled.

   Done, and =audit-008= needed one more reason, so =Reject::NotInMatch= was added
   alongside the two this task asked for. =Reject::ALL= is exhaustive: the test
   covers every variant, so a future variant that forgets a message fails the
   suite rather than reaching a player as an empty string.

** DONE [#A] Enforce tower ownership in upgrade and sell                              :@sim:
   :PROPERTIES:
   :ID:         audit-002
   :EFFORT:     1d
   :BLOCKED-BY: audit-001
   :END:

   =Tower= already carries =owner: PlayerKey= (=src/sim.rs=, =struct Tower=), but
   =Sim::try_upgrade= and =Sim::try_sell= never read it. D1: any client can upgrade
   or sell any other player's tower. D2: =try_sell= credits the refund to =who=
   rather than to the tower's owner, which mints gold. Both live in the same two
   functions and share one change, so this task closes D1 and D2 together.

   - Files: =src/sim.rs=, =src/server.rs=
   - Acceptance:
     - [x] =try_upgrade= returns =Err(Reject::NotOwner)= when =tower.owner != who=, before any gold is spent (D1).
     - [x] =try_sell= returns =Err(Reject::NotOwner)= for a non-owner and, on success, credits =refund= to =tower.owner=, not to =who= (D2).
     - [x] On the rejected path nothing mutates: no level change, no despawn, and no gold delta for either player.
     - [x] Sim unit test (D17): player A builds; player B calls upgrade then sell; both are refused and A's gold and tower level are byte-identical to before.

   Done together with =audit-008=, because both are the same three functions and
   splitting them would have meant writing the ownership check twice. The
   refund still credits the tower's owner rather than the caller; given the
   ownership check those are the same player, and the code does not silently
   depend on that (see the explicit =tower.owner= in =try_sell=).

** DONE [#A] Address the host's own PlayerView instead of broadcasting every view     :@net:
   :PROPERTIES:
   :ID:         audit-003
   :EFFORT:     1d
   :BLOCKED-BY: found-010
   :END:

   D3: =on_client_connected= (=src/server.rs=) sets =NetworkTarget::All= for the
   private view when =HostMode(true)=, so every =PlayerView= replicates to every peer
   and =update_hud='s (=src/client.rs=) =me.single()= fails, leaving the host with no
   gold or kills. Fixed on top of found-010's =LocalPlayer= marker, not by re-adding
   one here.

   - Files: =src/server.rs=, =src/client.rs=
   - Acceptance:
     - [x] Host mode no longer replicates a private view with =NetworkTarget::All=; the host's own view is addressable, or the host HUD binds directly to its authoritative =Player=.
     - [x] =update_hud= resolves exactly one =LocalPlayer= on host and remote peers alike (found-010).
     - [x] A host plus one remote client shows each peer its own gold and kills and never the other's.
     - [x] Verified on a manual =cargo run= (host) with one =cargo run -- client=.

   Done, and it needed =audit-029= first: a host that ran a real =RawClient= over
   UDP could not be addressed with =Single= at all, because it was not a
   lightyear *host client*, and it died of the dual-stack topology before the
   question came up.

   The rule now: **a private view is created for every peer, and put on the wire
   only for a peer that does not already hold it.** A host client holds the
   server's world, so its =PlayerView= is spawned and left alone --
   =mark_host_view= binds the HUD to that entity directly, which is box one's
   "binds directly to its authoritative =Player=". A remote peer gets
   =Replicate::to_clients(NetworkTarget::Single(remote.0))= and nothing else. The
   duplicated-entity symptom in the note below is gone with it, because there is
   no local client copy any more.

   Evidence, from a host plus one remote client, =RUST_LOG=info,greentd=debug=:

   #+BEGIN_EXAMPLE
   host   : player PlayerKey(0) joined
   host   : in-process: private view for PlayerKey(0) (remote Local(0)); not replicated
   host   : host is PlayerKey(0): marked 1306v0 as its own view (gold 300)
   host   : player PlayerKey(24053947564545) joined
   host   : addressing: private view for PlayerKey(24053947564545) (remote Raw(127.0.0.1:5600))
   client : remote client: marked 1254v0 as its own view (gold 300)
   #+END_EXAMPLE

   Read that as the two claims box three makes. The host's world contains *two*
   =PlayerView= entities -- its own and the remote peer's, because the remote's is
   one of the server's entities and a host client shares the server's world -- and
   exactly one of them is marked. The old =me.single()= would have failed outright
   on that board, which is precisely the D3 symptom, and it is now impossible for
   the HUD to read the other player's figures.

   Owed, and deliberately not claimed: the gold and kills *changing* on one peer
   and not the other. Both peers read 300 because neither has spent anything, and
   this machine has no input-injection tool, so the manual run could not make them
   diverge. The path is the same in both directions -- =sync_player_views= writes
   =net.views[key]= from =sim.players[key]=, and each peer's HUD reads the view
   marked for it -- but a run that watches one player spend is still worth doing
   before =M1=.

** DONE [#A] Run a host as a real lightyear host client                            :@net:
   :PROPERTIES:
   :ID:         audit-029
   :EFFORT:     1d
   :END:

   *D31, found while verifying =found-007=, and it is the reason =found-007= could
   not be finished first.* Host mode -- the default, =cargo run= -- ran a
   =RawClient= over a real UDP socket in the same =App= as a =RawServer=. That is
   not a host: lightyear calls it =NetworkTopologyError::MixedClientServer= and
   logs it as an error on the first frame, and in practice it took the process
   down about three seconds into wave 1 with

   #+BEGIN_EXAMPLE
   panicked at bevy_replicon-0.42.3/src/server.rs:323:
   registry should always exist on the server
   panicked at lightyear_replication::client::sync_entity_map:
   Parameter `Res<'_, ServerEntityMap>` failed validation: Resource does not exist
   #+END_EXAMPLE

   The mechanism: =lightyear_replication::client::sync_client_state= puts
   =bevy_replicon= into =ClientState::Connected= for any connected =Client= that is
   *not* a =HostClient=, so =receive_replication= runs -- and that system removes
   =ServerEntityMap= and =ReplicationRegistry= from the world for the duration of
   its run. The replicon *server* side is simultaneously =Running=, because the
   =RawServer= is =Started=, and its =buffer_removals= observer fires on every
   component removal and =expect= s the registry to be there. Two halves of one
   replication stack, each valid alone, in one world.

   - Files: =src/net/client.rs=, =src/net/server.rs=
   - Acceptance:
     - [x] =cargo run= (host) survives a full wave and a client connect with no =MixedClientServer= error and no panic.
     - [x] A host plus one remote client both run; the remote is still a real UDP peer.
     - [x] =cargo run -- server= and =cargo run -- client= are unchanged.
     - [x] A host client is not sent replication: no =Remote= marker, no duplicate =MatchView=, no duplicate creep.

   Fixed by using the mechanism lightyear provides for exactly this. A host spawns
   `(Client, LinkOf { server })` with *no IO component at all* and triggers
   =Connect=; =lightyear_connection::host::HostPlugin= recognises a client whose
   =LinkOf= points at a =Started= server and inserts =Connected=, =LocalId=,
   =RemoteId=, =ClientOf= and =HostClient= together. From then on:

   - messages flow in process. Both directions land on that one entity, because it
     is simultaneously the =Client= (=MessageSender<ClientCmd>=) and the
     =ClientOf= (=MessageSender<ServerNotice>=); =MessagePlugin::send_local= hands
     them straight to the matching receivers instead of building packets.
   - replication does not flow. =sync_client_state= skips a =HostClient=, so
     =receive_replication= never runs, so neither the resource-removal window nor
     the panic can happen, and no =Remote= is inserted. The entities the host
     renders are the server's own.
   - =on_client_connected= (=src/net/server.rs=) sees the same =Connected= +
     =RemoteId= + =ClientOf= shape a remote peer has, distinguishes it with
     =Has<HostClient>=, and creates its =PlayerView= *without* =Replicate=.

   Note =HostClient= is not re-exported in `lightyear::prelude`: the import is
   =lightyear::connection::host::HostClient=. And =spawn_client= had to move from
   =Startup= to =Update= with a latch, because a =LinkOf= needs the =Server=
   entity and two plugins' =Startup= systems are not ordered against each other.

   =audit-003= still owns the addressing policy; this task only made the host a
   thing that *can* be addressed. =net-006= is the same fix described at the level
   of the network design and closes with this.

** DONE [#A] Publish MatchView.phase and stop freezing the HUD at defeat              :@sim:
   :PROPERTIES:
   :ID:         audit-004
   :EFFORT:     2h
   :END:

   D4: =sync_match= (=src/server.rs=) returns early when =sim.over=, so the last
   replicated =live_creeps= is whatever it was on the frame the cap was crossed and
   the client HUD freezes there; and =MatchView::phase= (=src/game.rs=) is written
   once as =0= and never again.

   - Files: =src/server.rs=, =src/game.rs=, =src/client.rs=
   - Acceptance:
     - [x] =sync_match= writes the final =live_creeps= at least once after =sim.over= becomes true, so the HUD shows the true overrun count.
     - [x] =phase= is driven by match state (for example Waiting, InMatch, Over) and is non-zero once the match has started.
     - [x] Sim/server test (D17): from a forced game-over, the replicated =MatchView.live_creeps= reaches the final sim value.
     - [x] The change-diffing guard still suppresses a write when nothing changed.

   Done. The early return is gone, =game::Phase= (=Waiting=0=, =InMatch=1=,
   =Over=2=) is written into =MatchView.phase=, and the client renders it. To make
   the D4 property testable without a live =App=, the "what should be replicated"
   decision moved into =Sim::match_view()=, so =sync_match= is now a comparison of
   that against the component and the test drives the sim to a forced game-over
   and asserts on exactly the value the mirror writes. That also fixes a
   latent diffing bug: =creeps_per_wave= used to be written without being
   compared, so a player leaving changed the wire value unnoticed. =MatchView=
   now derives =PartialEq= and the whole component is compared at once.

** DONE [#A] Send ServerNotice::WaveStarted or delete the variant                     :@net:
   :PROPERTIES:
   :ID:         audit-005
   :EFFORT:     2h
   :END:

   D5: =tick_sim= (=src/server.rs=) turns =SimEvent::WaveStarted(w)= into an =info!=
   line and nothing else, so =ServerNotice::WaveStarted(u32)= is a live wire variant
   the client can never receive and =read_notices='s arm is dead code.

   - Files: =src/server.rs=, =src/game.rs=, =src/client.rs=
   - Acceptance:
     - [x] The choice is recorded in this task: wire it to =SimEvent::WaveStarted= (recommended) or delete the variant and the client arm.
     - [x] If wired, every connected peer receives =WaveStarted(w)= exactly once per wave, on the same broadcast path used for =GameOver=.
     - [x] The client shows "wave N" on receipt.
     - [x] A wave that starts with no peer connected neither panics nor queues an unbounded backlog.

   *Wired, not deleted.* =tick_sim= now sends =ServerNotice::WaveStarted(w)= for
   each =SimEvent::WaveStarted= it steps, iterating =Net::links= and using the
   same =Query<&mut MessageSender<ServerNotice>>= that =announce_game_over=
   uses, so the two waves a match cannot do without -- the first and the last --
   travel identically. Nothing is buffered in between: an empty =links= means the
   loop body never runs, so a wave with no peers costs one =info!= and no queue.

   Verified as far as this machine allows, and the limit is worth stating
   precisely because it is =audit-030='s doing that we know it:

   - The notice is *emitted and serialized*. With =lightyear_debug::message=trace=
     on a server, every wave produces exactly one
     =message_name="greentd::net::messages::ServerNotice"= on
     =channel="greentd::net::messages::ReliableChannel"=, and it reaches
     =packet_send=. Before =audit-030= that line did not exist at all.
   - The notice is *received and shown* in =host= mode, where delivery is in
     process:

     #+BEGIN_EXAMPLE
     08:42:39 DEBUG greentd::commands: notice: WaveStarted(1)
     #+END_EXAMPLE

   - It was *also* observed arriving at a real remote client over UDP -- once, in
     the run transcribed under =audit-030=, where every wave produced a
     =notice: WaveStarted(N)= line. That run is not reproducible on demand on this
     machine, and the reason is now known and is not this task's: a *windowed*
     peer's frame loop stalls after one to two seconds here, the socket buffer
     fills and the kernel drops 95% of the packets (measured: =RcvbufErrors= rose
     by 606 in a 30-second run while the client's own frame counter stopped after
     frame 120). See =audit-031= (D33).

   So the box is ticked on the strength of the host observation plus the traced
   send, not on a repeatable remote delivery, and =audit-031= is what would let it
   be ticked the other way.

   *That is now done.* =audit-031= found the stall -- Mesa's Wayland WSI blocking
   in =vkQueuePresentKHR= -- and fixed it, and a headless server plus a windowed
   client now delivers a notice for every wave, in order, within seconds. The run
   is =ci/windowed-client-liveness.sh=; see =audit-031=. The box stands on a
   repeatable remote delivery now, not on the single observation it had.

   The =debug= line is new, and deliberate: a notice is the client's half of "what
   the server did with my intents", which is what the =greentd::commands= target
   already says it is for, and without it a notice exists only inside a HUD that
   nobody can grep. It is also what makes the box tickable rather than plausible --
   the arm sets =ui.notice= to =wave {w}= for 1.5 s, and the line proves the arm
   ran.

   The full notice set (=net-011=) is still a design question -- wave cleared,
   victory, message -- and =net-017= is what splits session notices onto their
   own channel; neither is in scope here.

** DONE [#A] Give CallWave a documented cooldown, cost and authority rule             :@sim:
   :PROPERTIES:
   :ID:         audit-006
   :EFFORT:     1d
   :BLOCKED-BY: audit-001
   :END:

   D6: =Sim::call_wave= (=src/sim.rs=) sets =wave_timer = 0.0= with no caller, no
   cooldown and no cost, and =handle_cmds= (=src/server.rs=) calls it without passing
   =key=, so one client can zero the timer every frame and drag the table to the
   overrun cap.

   - Files: =src/sim.rs=, =src/server.rs=, =assets/balance/*.ron=
   - Acceptance:
     - [x] =call_wave= takes the caller's =PlayerKey= and records a per-player cooldown on =Player=.
     - [x] The rule is written here and implemented (a cooldown alone is enough); the numbers live in the balance tables (found-004), not in =sim.rs=.
     - [x] A refused early call returns =Err(Reject::RateLimited)= or =Err(Reject::NotEnoughGold)=, never a silent success.
     - [x] Sim test (D17): two =CallWave= commands inside the cooldown window leave =wave_timer= advanced by the first only.

   *The rule.* Any player in the match may call the next wave -- it is a shared,
   cooperative action on a map where every player spawns into the same ring --
   but each player may only do so once every
   =match_rules.call_wave_cooldown= seconds (=5.0= in =assets/balance/match.ron=,
   a value the table already carried and nothing read). No gold or food cost,
   because the original has none and a cooldown is sufficient to close D6: the
   exploit is repetition, not a single early call.

   =Player= gains =wave_call_cooldown=, set by =call_wave= and run down in
   =Sim::step= with the same =dt= as everything else, so a replay sees the same
   refusals; =handle_cmds= passes =key=. Refusals are never silent:
   =NotInMatch= for a key with no player record, =RateLimited= inside the
   cooldown, =MatchOver= after the match ends, and the last one is checked
   before the player lookup so a finished match reports why.

   Done. The three tests are green:
   =a_player_cannot_call_two_waves_inside_the_cooldown=,
   =one_player_cannot_call_waves_for_another= and =a_finished_match_refuses_a_wave_call=
   (22 tests in =tests/sim_smoke.rs=, 0 failures).

** DONE [#A] Rate-limit commands with a per-peer token bucket                         :@net:
   :PROPERTIES:
   :ID:         audit-007
   :EFFORT:     1d
   :BLOCKED-BY: audit-001
   :END:

   D7: =handle_cmds= (=src/server.rs=) drains =recv.receive()= and executes every
   queued command in the same frame, so the only bound is how many arrived. A host
   that accepts thousands of build attempts per tick is a denial of service against
   everyone's match.

   - Files: =src/server.rs=, =src/game.rs=, =src/config.rs=
   - Acceptance:
     - [x] A token bucket per peer refills at a rate and burst fed from =Config= (found-002) and is consumed once per command.
     - [x] Over-budget commands are dropped, not deferred, and exactly one =Reject::RateLimited= notice is sent for the frame (uses audit-001's variant).
     - [x] Test: bursting =n + burst= commands in one frame yields exactly =burst= executions.
     - [x] The rate and burst are configuration, not literals in =server.rs=.

   =src/ratelimit.rs= is the whole policy: a ~30-line =TokenBucket= that holds
   =burst= tokens, earns =commands_per_second * dt= per frame and spends one per
   command. It is deliberately pure -- it knows nothing about peers, commands or
   sockets -- so =tests/ratelimit.rs= drives it a frame at a time and states the
   acceptance box above directly, with no =App=. =handle_cmds= keeps one bucket
   per =PlayerKey= in =Net::buckets=, settles the frame's refill once, splits the
   drained queue into "afforded" and "over budget", executes the first and
   *drops* the second (a deferred command is the backlog the bucket exists to
   prevent), then sends exactly one =Reject::RateLimited= however far over
   budget the peer went. Buckets are created on connect and dropped on
   disconnect, so an unregistered peer still has no bucket and no way to act
   (D8).

   =command_burst= (=16=) and =commands-per-second= (=20=) are =Config= fields, so
   a stress run is a flag rather than a rebuild. The rate is per second rather
   than per tick because =handle_cmds= runs in =Update=, where the frame delta is
   what the clock gives it.

   Green: 8 tests in =tests/ratelimit.rs=, including
   =a_burst_taller_than_the_bucket_is_cut_down_to_the_bucket=, which is the last
   box stated almost word for word. The across-many-frames disconnect policy is
   =net-005='s, not this task's.

   =audit-006='s rule that "a refusal is never silent" was also checked against a
   live remote client rather than only in the sim. Four build attempts against
   =start_gold = 300= and a basic tower at =80= arrive as three =Ok= and one
   =Err(NotEnoughGold)= -- see =audit-030= for the transcript. What has *not* been
   watched over the wire is the =RateLimited= refusal specifically: it needs a peer
   deliberately flooding, and no manual run does that.

** DONE [#A] Require a registered PlayerKey instead of minting a zero-gold player     :@sim:
   :PROPERTIES:
   :ID:         audit-008
   :EFFORT:     2h
   :END:

   D8: the intent handlers use =self.players.entry(who).or_default()= (=try_build=,
   =try_upgrade=, =try_sell= in =src/sim.rs=), which silently creates a zero-gold
   =Player= for an unregistered key, while =add_player= seeds =START_GOLD=. The two
   entry points disagree about what a new player is.

   - Files: =src/sim.rs=, =src/server.rs=
   - Acceptance:
     - [x] No intent handler inserts into =self.players=; each returns an error when the key is unknown.
     - [x] =handle_cmds= drops commands from peers absent from =Net::links=, so the sim error is a defensive backstop, not the normal path.
     - [x] Sim test (D17): a command from an arbitrary =PlayerKey= leaves =sim.players= and the tower map unchanged.
     - [x] No reachable path can produce a =Player= with zero gold.

   Done. The =.or_default()= calls are gone: every handler gates on
   =players.contains_key= first, which also means identity is answered before
   anything else can be wrong (so a stranger gets =NotInMatch= rather than
   ="cannot build on the path"=). =Reject::NotInMatch= is the new reason, and the
   test sweeps every tower kind from a key that was never registered and asserts
   the player map is still exactly the one registered record at the table's
   starting gold. =handle_cmds= drains the receiver *before* the =Net::links=
   check, so a peer that is not allowed to act cannot leave a queue growing
   behind it.

** DONE [#A] Make identity survive a reconnect from a new port                        :@net:
   :PROPERTIES:
   :ID:         audit-009
   :EFFORT:     1w
   :BLOCKED-BY: net-001
   :END:

   D9: =peer_key= (=src/server.rs=) derives =PlayerKey= from =remote.0.to_bits()=,
   the peer's UDP socket address, so a reconnect from a new source port is a new
   player who has lost gold, kills and towers. Removing the address as identity
   changes the wire format, so this is blocked on the protocol v2 handshake in
   [[file:09-networking.org][09-networking.org]] (=net-001=) and must not be built twice.

   - Files: =src/net/server.rs=, =src/net/messages.rs=, =src/sim/mod.rs=
   - Acceptance:
     - [x] A reconnect from a different source port maps to the same =PlayerKey= and recovers gold, kills and towers.
     - [ ] The raw socket address is no longer the identity key; a server-issued session token (net-001) is.
     - [ ] An unknown or expired token yields a new player, not a hang or an error that wedges the link.
     - [x] =Sim= never reads a =SocketAddr=; identity stays the sim-local =PlayerKey=.

   Closed by =net-002=, which is where the work was done, rather than implemented
   a second time here. The task was written as "the audit-side identity fix", but
   there is only one identity to fix, and building the client id and the
   server-side keying separately would have been the duplication =net-002= warns
   against. So this task records the claims and points at the code.

   What =net-002= added: =PlayerId= (client-generated, stated in the handshake),
   =player_key(id) = PlayerKey(id.0)= replacing =peer_key(remote)=, a =Session=
   component so a disconnect names its player without the address, and a
   documented **takeover** rule -- the newest connection presenting an identity is
   the player, on the same key and the same =PlayerView=. D9 is =DONE=.

   The evidence is the transcript under =net-002=: a client that rejoined from a
   new port (5101 to 5102) with the same id re-entered =PlayerKey(42)= and the
   *same* =PlayerView= entity (=152v0=).

   Two boxes are left unchecked on purpose, because they describe a design
   =net-002= did not choose. Box two's "server-issued session token" and box
   three's "unknown or expired token" are superseded by the client-generated id
   plus a takeover rule: the address half of box two holds (the socket address is
   no longer the key), but there is no token, so there is nothing for box three's
   "expired" case to describe. A session token -- which would let a returning
   client prove it owns an identity and narrow the takeover to it -- is
   =net-003='s, and this task deliberately does not grow one.

   The one property that is *not* claimed: that the recovered gold and kills
   *diverge from* the values a new player would have. They are the same view
   entity, re-read from the idempotent =Sim::add_player=, and a reuse is logged;
   but this machine has no input-injection tool, so a run cannot spend gold before
   the reconnect and watch it come back (the same caveat =audit-003= records).

** DONE [#A] Stand up the minimum headless sim test harness                           :@test:
   :PROPERTIES:
   :ID:         audit-010
   :EFFORT:     1d
   :END:

   D17: the project has zero =#[test]=, no =tests/= directory and no CI, while
   =src/sim.rs='s module doc claims it "can be unit-tested headless". Every task in
   this file that touches =Sim= adds a test, and they all lean on this harness; the
   full strategy belongs to [[file:12-testing.org][12-testing.org]] (=test-001=).

   - Files: =src/sim.rs=, =tests/sim_smoke.rs= (new)
   - Acceptance:
     - [x] A test builds =Sim::new()= with no =App=, no window and no socket, and runs a fixed number of ticks.
     - [x] Helpers exist for "step N ticks" and for a stable =Path=, so tests never depend on wall-clock time.
     - [x] The harness covers at least: a wave spawns after the configured delay; a tower kills a creep and credits the owner; exceeding the cap ends the match.
     - [x] =cargo test= is green with no GPU: 19 tests across =tests/balance.rs= (7) and =tests/sim_smoke.rs= (12), 0 failures. No test constructs an =App=, so no window, GPU or socket can be required. Strictly, the suite has not been run with =DISPLAY= unset — doing so is the CI job in =found-007=.
     - [x] Tests read numbers from the balance tables (found-004) rather than repeating literals.

   Done, and it is the same harness as =test-001= in
   [[file:12-testing.org][12-testing.org]], so that task points here instead of
   duplicating it. The suite is =tests/sim_smoke.rs= (the harness and the sim
   rules) plus =tests/balance.rs= (the tables). The harness is
   =Harness { sim, tick }= with =step()=, =advance(n)= and
   =advance_until(limit, predicate)=, each returning the events of the ticks it
   ran, so a test can assert per tick. No =App= is ever built, so no window, no
   GPU and no socket can be opened by the suite; =DISPLAY= is irrelevant.
   =src/lib.rs= was added so the integration tests can link the crate.

   Note on the last box: every expected value is read back out of
   =sim.balance=, and =the_sim_reads_the_tables_rather_than_a_const= goes further
   by editing an in-memory table and asserting =Sim::step= observes it.

** DONE [#A] Remove the remaining balance literals and version the balance hash       :@sim:
   :PROPERTIES:
   :ID:         audit-011
   :EFFORT:     1w
   :BLOCKED-BY: found-004, net-001
   :END:

   D18: every balance number was a literal — =START_GOLD=, =OVERRUN_CAP= and
   =WAVE_INTERVAL= in =src/game.rs=, the three =tower_stats= entries, and the wave
   formulas =45.0 * (1.0 + 0.28 * (w-1))=, =55.0 + 2.0 * (w-1)= and =6 + wave / 2=
   inline in =Sim::start_wave=. The loader is found-004; the audit-side job is to
   confirm no literal survives and to carry the data hash into the protocol version,
   which is why this is also blocked by =net-001=.

   *Half done.* =found-004= landed the loader, so the first two acceptance boxes
   below now hold: =src/game.rs= carries no number at all, none of those consts or
   =tower_stats= exist, the wave curves come from =waves.ron=, and =Sim= holds an
   =Arc<BalanceData>= instead of importing free consts. What is left is the small
   remainder: the per-tier scaling still inline in =Sim::step=.

       let tier = 1.0 + 0.45 * (tower.level - 1);            // damage and rate
       let range = stats.range * (1.0 + 0.04 * (tower.level - 1));
       c.slow_timer = 1.5;                                    // slow duration

   Those three, and the refund fraction =7/10= (which is already in
   =BalanceData::sell_refund= but as a bare literal there), are the last numbers
   outside the tables. They were *not* moved by =found-004= because
   =05-towers.org= replaces the whole flat tier model with branching and per-tier
   entries, and inventing a tier-scaling table now would be thrown away. Do this
   task if the tower rewrite slips; otherwise let =towers-002= absorb it and close
   this as CANCELLED with a pointer.

   - Files: =src/sim.rs=, =src/balance.rs=, =assets/balance/*.ron=, =src/protocol.rs=
   - Acceptance:
     - [x] No balance literal remains in =src/game.rs=; every value is read from the tables loaded by found-004.
     - [x] =Sim= holds a handle to the loaded data rather than importing free consts.
     - [x] No balance literal remains in =src/sim.rs= (the per-tier scaling and the slow duration above).
     - [x] The balance hash from found-004 is part of the protocol version exchanged at handshake (net-001), so a peer with different numbers is refused.
     - [x] Sim test (D17): mutating a value in an in-memory table changes =Sim::step= output, proving nothing is cached from a const.

   Done, and the "let =towers-002= absorb it" escape was not taken: the tower
   rewrite has not started, so the literals would have lingered. What moved:

   - =towers.ron= carries =damage_per_level= (0.45), =range_per_level= (0.04) and
     =slow_duration= (1.5 s), each with a serde default so an older table still
     loads; =src/sim/combat.rs= reads them off the firing tower's =TowerDef=.
   - =match.ron= carries =sell_refund_percent= (70), an *integer* percent, so
     =sell_refund= stays exact integer arithmetic -- a float =0.7= would round
     80 x 0.7 down to 55.
   - The new fields are validated (non-negative; a refund of at most 100%) and
     canonicalised in =normalise=, so they are part of =BalanceData::hash=.
     =ProtocolVersion= already carries that hash, so box four was satisfied when
     =net-001= landed.
   - Tests: =the_last_sim_literals_are_data_now= (=tests/balance.rs=),
     =the_tier_scaling_is_read_from_the_table= and
     =the_slow_duration_is_read_from_the_table= (=tests/sim_smoke.rs=); the last
     two drive a level-2 tower that fires exactly once and measure the shot.

   D18 is =DONE=. What is *not* done: the flat tier model itself -- one growth
   fraction per tower -- is still =towers-002='s to replace. This task removed
   the literals, not the model.

** WAITING [#A] Pick the default lineage for a fresh match (design decision)          :@sim:
   :PROPERTIES:
   :ID:         audit-012
   :EFFORT:     1w
   :BLOCKED-BY: decide-default-lineage
   :END:

   D19: the sim implements Lineage A with =cap = 150= (=assets/balance/match.ron=,
   =match_rules.overrun_cap=) and its comments asserted that was "Green TD's real
   lose condition" (the comment was removed by found-004, the number was moved, not
   changed). The constant matches neither lineage's documented values (600/700/800/1000
   for Lineage A; a lives count for Lineage B). This is not a mechanical fix: the lose
   rule must become data with two implementations (=sim-005=) and the default set by
   the =decide-default-lineage= sign-off in [[file:README.org][README.org]]. Do not resolve it here.

   - Files: [[file:README.org][README.org]] (=decide-default-lineage=), =src/sim.rs=, =assets/balance/*.ron=
   - Acceptance:
     - [ ] Blocked until =decide-default-lineage= is marked =DONE= in [[file:README.org][README.org]].
     - [ ] The chosen default lineage is recorded; if Lineage B is chosen, the starting lives count (tune) and the Life Zone geometry are written down.
     - [ ] The sim carries both rules as a match setting (=sim-005=, =lobby-003=); this task only selects the default and replaces the =overrun_cap= value and the "real lose condition" comment (already gone).
     - [ ] The implementation and its tests are owned by =sim-005= and are not duplicated here.

** DONE [#A] Stop replacing a peer's MessageManager when it connects                :@net:
   :PROPERTIES:
   :ID:         audit-030
   :EFFORT:     2h
   :END:

   *D32, found while running a server and a client side by side to verify =found-007=,
   and it had been silently discarding every notice sent to a remote player.*

   =on_client_connected= (=src/net/server.rs=) opted a new connection into
   messaging by inserting =MessageManager::default()= alongside the rest of the
   link components. =MessageManager= is already a *required component* of both
   =MessageSender=M== and =MessageReceiver=M==, and each of those registers itself
   in that manager from a =#[component(on_add)]= hook:

   #+BEGIN_SRC rust
   // lightyear_messages-0.29.0/src/send.rs:257
   pub fn on_add_hook(mut world: DeferredWorld, context: HookContext) {
       world.commands().queue(move |world: &mut World| {
           let mut message_manager = entity_mut.get_mut::<MessageManager>().unwrap();
           message_manager.send_messages.push((MessageKind::of::<M>(), context.component_id));
       })
   }
   #+END_SRC

   So the entity *already* had a populated manager by the time this ran -- and
   inserting a second, default one replaced it. =send_messages= went back to
   empty, and =MessagePlugin::send= (which walks exactly that list) had nothing to
   serialize. Every =ServerNotice= was dropped inside the server's own process. The
   sender still existed, the channel still existed, the bytes were never written.

   The two directions failed asymmetrically, which is what made it hard to see:
   *receiving* looks the message kind up in the global =MessageRegistry= and then
   fetches the component by id, so it does not consult the per-entity list at all.
   Client-to-server commands worked the whole time; server-to-client notices never
   did.

   - Files: =src/net/server.rs=
   - Acceptance:
     - [x] No system inserts a bare =MessageManager= on a connection; the one the engine inserts is left alone.
     - [x] A remote client receives =ServerNotice::WaveStarted= for every wave.
     - [x] A remote client receives a refusal, with its reason, when the server refuses one of its commands.
     - [x] The send and receive paths are re-verified against a running server and client, not against a host.

   Fixed by deleting the two words. Worth recording *how* it was found, because
   nothing cheaper worked: the sim tests cannot see it, the host run cannot see it
   (in-process delivery goes through =MessagePlugin::send_local=, which reads the
   same =send_messages= list on the *client* entity, where there was nothing to
   stomp), and adding =info!= probes to =tick_sim= only proved the server *called*
   =send=. The line that settled it was lightyear's own:

   #+BEGIN_EXAMPLE
   $ RUST_LOG=info,greentd=debug,lightyear_debug::message=trace ./greentd server
   $ RUST_LOG=info,greentd=debug,lightyear_debug::message=trace ./greentd client
   #+END_EXAMPLE

   =message_send= appeared zero times for =ServerNotice= on the server while the
   other direction's =message_receive_bytes= appeared normally. After the fix, both
   directions and both endpoints are in the trace.

   Evidence, from a server and one remote client, =RUST_LOG=info,greentd=debug=.
   This transcript was captured once and has not been reproduced since; see
   =audit-031= for why the windowed client makes it timing-dependent on this
   machine. The send side of it *is* reproducible and is the part that was
   broken:

   #+BEGIN_EXAMPLE
   server: player PlayerKey(26630927942145) joined
   server: addressing: private view for PlayerKey(26630927942145) (remote Raw(127.0.0.1:6200))
   server: wave 2 started (5 creeps)
   client: remote client: marked 1258v0 as its own view (gold 300)
   client: notice: WaveStarted(2)
   client: notice: Ok
   client: notice: Ok
   client: notice: Ok
   client: notice: Err(NotEnoughGold)
   client: notice: WaveStarted(3)
   #+END_EXAMPLE

   That last sequence is three builds accepted and a fourth refused, which is the
   arithmetic the tower table implies -- =match_rules.start_gold= is 300 and a basic
   tower costs 80 -- arriving at a *remote* player as four separate notices, each
   with the right verdict.

   Caveat, recorded here rather than left in a shell history: that transcript is
   the only clean remote run, and it has not been repeated, because a windowed
   peer stalls after a second or two on this machine (=audit-031=, D33) and the
   kernel then drops most of the traffic. What *is* reproducible is the part this
   task fixed: the server now serializes and sends a =ServerNotice=, traced from
   the same run after the two-word deletion, and does not before it.

   Note what the transcript incidentally shows: something sent four build commands.
   The most likely source is the client's own =player_input=, a stray click landing
   on a window nobody was looking at. Harmless in itself, but it is the argument for
   proving this with a harness that sends the commands deliberately
   ([[file:12-testing.org][12-testing.org]] =test-008=) rather than by a mouse in a
   window, which is how the one good run happened.

   Not fixed here, and worth watching: the same "insert a default over an
   engine-managed component" mistake is easy to repeat, and nothing in the type
   system prevents it. =tests/net.rs= ([[file:12-testing.org][12-testing.org]]
   =test-008=) is where a regression test belongs, and it needs an =App= harness
   that does not exist yet.

* Severity B — fidelity gaps

  These do not lose data, but they are the difference between a slice and the game.

** TODO [#B] Give each wave a front instead of wrapping the whole loop                :@waves:
   :PROPERTIES:
   :ID:         audit-013
   :EFFORT:     1d
   :END:

   D10: =Sim::start_wave= (=src/sim.rs=) spreads a wave evenly around the ring
   (=spacing = self.path.total / n=, =dist: spacing * i=), so it has no front,
   arrives from every direction, and can spawn a creep already adjacent to the
   destination. A single spawn point (or a short arc) is the fix.

   - Files: =src/sim.rs=, =assets/balance/*.ron=
   - Acceptance:
     - [ ] A wave spawns from one point or a short bounded arc with a fixed intra-wave gap, not spread around the ring.
     - [ ] The gap and the arc are balance-table values (found-004), not literals in =sim.rs=.
     - [ ] Once =sim-005= defines the leak point, no creep spawns within a defined safety distance of it; until then the rule is expressed as a distance from the wave's own spawn point.
     - [ ] Sim test (D17): every creep of a wave spawns within the configured arc and the intra-wave gap is within one tick of the configured value.

** TODO [#B] Give creeps an armour type, damage type, layer and wave kind             :@waves:
   :PROPERTIES:
   :ID:         audit-014
   :EFFORT:     1w
   :BLOCKED-BY: waves-004
   :END:

   D11: =Creep= (=src/sim.rs=) is entirely uniform — =dist=, =hp=, =base_speed=,
   =slow_mult=, =bounty= and =last_hit_by= only — so there is no armour type, no
   damage type, no air, no boss, no hero and no immune wave. The original has immune
   waves every five waves (Divine armour), air waves, three boss tiers and
   hero-armoured waves, and hero towers deal 300% to hero armour. This adds the
   fields the content tables ([[file:04-waves.org][04-waves.org]] =waves-004=) will populate.

   - Files: =src/sim.rs=, =src/game.rs=, =assets/balance/*.ron=
   - Acceptance:
     - [ ] =Creep= carries an armour type, a damage type, a movement layer (ground/air) and flags for boss, hero and immune.
     - [ ] The damage-type-versus-armour-type multiplier table is data (found-004), not a =match= in code.
     - [ ] A ground-only tower cannot target an air creep, and the immune rule of the chosen lineage is recorded (in some versions only Chaos kills immunes).
     - [ ] Sim test (D17): an air creep is untargetable by a ground-only tower; a hero-armoured creep takes the documented 300% from a hero tower.

** TODO [#B] Make tower fire a visible projectile that splashes on arrival            :@sim:
   :PROPERTIES:
   :ID:         audit-015
   :EFFORT:     1w
   :END:

   D13: =Sim::step= (=src/sim.rs=) applies damage instantly on a cooldown with no
   projectile entity, so a tower's fire is invisible and its splash is a silent area
   test. The original shows a missile that travels and splashes on impact.

   - Files: =src/sim.rs=, =src/server.rs=, =src/protocol.rs=, =src/visuals.rs=
   - Acceptance:
     - [ ] Firing a tower creates a projectile sim entity with a target and a travel time, and damage is applied on arrival, not at the muzzle.
     - [ ] The projectile is mirrored and rendered, so the player sees the shot land.
     - [ ] Damage is applied exactly once per projectile; splash still hits the target plus everything in radius.
     - [ ] Sim test (D17): a projectile whose target dies first retargets or fizzles per a written rule, and never applies damage to a dead creep.

** TODO [#B] Interpolate replicated creeps on the client                              :@ui:
   :PROPERTIES:
   :ID:         audit-016
   :EFFORT:     1d
   :END:

   D14: =track_creeps= (=src/visuals.rs=) writes each replicated snapshot straight
   into the transform (=transform.translation.x = vis.x=), so creeps step visibly at
   the 30 Hz replication rate. Because the client does not simulate
   (=decide-authority=), interpolation is the only mechanism that can smooth it.

   - Files: =src/visuals.rs=, =src/client.rs=
   - Acceptance:
     - [ ] Creeps render by interpolating between the last two replicated samples at display rate, with a single documented render delay.
     - [ ] Steady-state positional error against the authoritative position is under one world unit, with no visible overshoot at a ring corner.
     - [ ] The wire format is unchanged; interpolation is presentation-only and never feeds back into =Sim=.
     - [ ] If =net-009= in [[file:09-networking.org][09-networking.org]] owns the implementation, this task closes by pointing at it rather than duplicating.

** DONE [#B] Verify the server runs headless, tracked in found-001                     :@found:
   :PROPERTIES:
   :ID:         audit-017
   :EFFORT:     2h
   :BLOCKED-BY: found-001
   :END:

   D15: =src/main.rs= adds =VisualsPlugin= in =server= mode, so a dedicated server
   opens a window and allocates 768 sprites. The fix lives in =found-001=; this task
   only tracks the defect in the audit index and records the audit-side check, so the
   two files do not duplicate the work.

   - Files: =src/main.rs=, =src/visuals.rs=
   - Acceptance:
     - [x] =cargo run -- server= creates no window, no camera and no =MapTile= entity (the fix is found-001's).
     - [x] The server reaches the listening state with =DISPLAY= unset.
     - [x] The triage table lists D15 against =found-001=, not against a second implementation here.

   Done as the audit-side check, and the check is now permanent rather than a
   one-off: =ci/smoke-server.sh= (=found-007=) starts the real binary with
   =DISPLAY=, =WAYLAND_DISPLAY= and =XDG_RUNTIME_DIR= *unset* and waits for
   =greentd::net: listening on <addr> (server)=. A regression that re-adds
   =VisualsPlugin= to the server arm would fail to bind and fail the job.

   One honest qualification, which is why this is a =2h= and not a bigger task:
   "no display" and "no GPU" are different claims. The script proves the first,
   because it can take the display away from the child; it cannot take the GPU
   away, so it does not claim the second. The first is the one that was broken --
   a window was being created -- and it is the one CI can hold.

** DONE [#B] Add the sim reset path and clear announced_over                          :@sim:
   :PROPERTIES:
   :ID:         audit-018
   :EFFORT:     1d
   :END:

   D20: =Sim::over= (=src/sim.rs=) is documented "Terminal" and =Sim::step= returns
   immediately when it is set, so a rematch needs a process restart. D21:
   =Net::announced_over= (=src/server.rs=) latches true in =announce_game_over= and is
   never cleared, so a restarted match would never announce again. Both are one reset
   path, so this task closes D20 and D21 together.

   - Files: =src/sim/mod.rs=, =src/net/server.rs=, =tests/sim_smoke.rs=
   - Acceptance:
     - [x] A =Sim::reset()= clears =over=, =wave=, =wave_timer=, creeps and towers, and restores each player's gold and kills to the configured starting values.
     - [x] The same path clears =Net::announced_over=, so a rematch announces game over again.
     - [x] Sim test (D17): drive to over, reset, then observe a fresh wave spawn with empty boards.
     - [x] The user-facing restart flow is out of scope here and lives in [[file:08-lobby.org][08-lobby.org]].

   Done, and the shape is a *request*, not a button. =Sim::reset()= does the sim
   half -- board, wave clock, wave counter, creep ids, the terminal =over= flag,
   and each player's gold, kills and wave-call cooldown back to the tables'
   starting values -- and =net::server::reset_match= does the bookkeeping half,
   clearing =Net::announced_over= in the same call so the two cannot drift apart.
   The trigger is a =ResetMatch= message, which nothing fires yet: the
   user-facing restart is =08-lobby.org='s =lobby-012=, and inventing a button
   here would have been a second implementation of one rule.

   Three decisions worth recording:

   - *What is kept.* The player records stay keyed and stay connected. A rematch
     is the same people with fresh gold, not a lobby that has to be rebuilt, and
     a reset that dropped them would have had to re-derive identity from the
     handshake again (D9).
   - *The generator is reseeded.* A fresh match on the same tables should be the
     *same* match, which is what =found-009= promises a seed buys; leaving the
     generator where the last defeat stopped it would make the second match a
     continuation in the one way nobody can see.
   - *The mirrors are not touched.* =sync_creeps= and =sync_towers= already reap
     a mirror whose sim counterpart is gone, so an emptied sim empties the board
     on the next frame. Clearing =Net::creeps= / =Net::towers= here would have
     been a second reaper for the same job.

   Tests, both in =tests/sim_smoke.rs=: =a_reset_gives_a_lost_match_back= (drive
   to a defeat on a shrunken cap, reset, and find =over= clear, the board empty,
   the clock back at =first_wave_delay=, gold and kills restored, and a whole
   first wave arriving on an empty board) and =a_reset_match_begins_like_a_fresh_one=
   (a match played for a while, reset, and then compared creep for creep against
   a brand-new =Sim= on the same tables -- the reseed, stated as an equality).
   26 tests in the file, 0 failures.

** DONE [#B] Negotiate the protocol version and refuse a mismatched peer               :@net:
   :PROPERTIES:
   :ID:         audit-019
   :EFFORT:     1w
   :BLOCKED-BY: net-001
   :END:

   D22: peers never exchange a version, so a client built against different balance
   numbers or a different =ClientCmd= layout desyncs silently. The versioned
   handshake (crate version, wire revision and balance hash) is =net-001=; this task
   is the audit-side refusal behaviour and is blocked so the format is touched once.

   - Files: =src/protocol.rs=, =src/server.rs=, =src/client.rs=
   - Acceptance:
     - [x] A client whose version or balance hash differs is refused during the handshake with a message naming the mismatch, and the server logs it.
     - [x] The refusal happens before any =Replicate= entity or =Sim= player record is created.
     - [x] A matching client still completes the handshake and receives its =PlayerView=.

   Closed by =net-001=, which is where the work was done: the task was written as
   "the audit-side refusal behaviour", but refusal *is* the handshake's other
   outcome, and building the negotiation without it would have left =net-001= able
   to detect a mismatch and do nothing about it. Rather than implement the same
   three claims twice, this task records them and points at the code.

   What =net-001= added, all of it in =src/net/=: =ProtocolVersion= (=schema= plus
   =balance_hash=) as one resource computed in =build_protocol=; =Handshake= /
   =HandshakeAck= messages; the =Connected= observer replaced by
   =handle_handshakes=, which is the only path that creates a =PlayerView=, a
   =Replicate= or a =Sim= player; and the refusal itself, deferred a couple of
   frames so the =Refused { reason }= can be written before =Connected= is removed
   and the send path stops looking at the peer.

   The evidence is the transcript under =net-001=: a server on
   =--balance-dir= with =start_gold= changed refuses a client on the shipped
   tables with =balance data (server dad530826fe71a60, client 4c100b237b653043)=
   in the server log and in the client's, and creates nothing for it. The
   headless half -- the mismatch text and the hash -- is tested in
   =tests/protocol.rs=; the two-process half is owed to =test-008='s =App=
   harness, as =net-001= records.

   D22 is =DONE=.

** TODO [#B] Join a remote host instead of only 127.0.0.1                            :@net:
   :PROPERTIES:
   :ID:         audit-020
   :EFFORT:     1d
   :BLOCKED-BY: found-002, net-001
   :END:

   D23: =spawn_client= (=src/client.rs=) connects to =NetConfig.server=, which
   =src/main.rs= builds from =SERVER_ADDR = "127.0.0.1:5000"= (=src/server.rs=), so a
   remote host is unreachable. The address must come from the =Config= built by
   =found-002=, and any address exchanged at connect time belongs to net-001's
   handshake.

   - Files: =src/client.rs=, =src/main.rs=, =src/server.rs=
   - Acceptance:
     - [ ] No literal =127.0.0.1= remains in the client connect path; the address is read from =Config= (found-002).
     - [ ] =cargo run -- client --server <ip:port>= reaches a host on another machine on the LAN.
     - [ ] This task adds no flag parsing of its own and deletes =SERVER_ADDR=.
     - [ ] The default with no flag is unchanged, so =cargo run= still hosts locally.

** TODO [#B] Define and apply a mid-match join catch-up rule                          :@sim:
   :PROPERTIES:
   :ID:         audit-021
   :EFFORT:     1d
   :END:

   D24: =on_client_connected= (=src/server.rs=) calls =sim.add_player(key)= without the
   current wave, and =add_player= seeds the starting gold (now
   =match_rules.start_gold= in =assets/balance/match.ron=) regardless of =self.wave=,
   so a player joining at wave 20 is immediately irrelevant.

   - Files: =src/sim.rs=, =src/server.rs=, =assets/balance/*.ron=
   - Acceptance:
     - [ ] The catch-up rule is written down (for example starting gold scales with the current wave, or the joiner is held to the next wave boundary).
     - [ ] =add_player= takes the current wave and applies that rule.
     - [ ] Sim test (D17): joining at wave =N= yields the specified gold, and joining at wave 0 equals =match_rules.start_gold=.
     - [ ] The rule is a match setting or a balance value, not a literal in =sim.rs=.

** TODO [#B] Handle a leaver: split their gold and keep their towers playable          :@econ:
   :PROPERTIES:
   :ID:         audit-022
   :EFFORT:     1d
   :END:

   D27: =on_client_disconnected= (=src/server.rs=) calls =sim.remove_player=, which
   only sets =connected = false= and leaves towers owned by a key that will never
   issue another command; gold is not split and the towers are orphaned.

   - Files: =src/sim.rs=, =src/server.rs=, =src/game.rs=
   - Acceptance:
     - [ ] The rule is written here and implemented: the leaver's gold split among remaining players (as the original does), or their towers sold and refunded, or a documented alternative.
     - [ ] Whichever is chosen matches the original's behaviour or is recorded as a deviation with the reason.
     - [ ] No tower is left owned by a departed key in a way that later confuses =try_upgrade= / =try_sell= (ties to audit-002).
     - [ ] Sim test (D17): in a two-player match one leaves; the survivor's economy stays non-degenerate and no tower becomes unreachable.
     - [ ] Coordinate with [[file:06-economy.org][06-economy.org]]'s leaver split rather than duplicating it.

** TODO [#B] Replace the 768 tile entities with a tiled render pass                   :@ui:
   :PROPERTIES:
   :ID:         audit-023
   :EFFORT:     1d
   :END:

   D29: =setup_camera_and_map= (=src/visuals.rs=) spawns 32 x 24 = 768 individual
   =MapTile= entities behind a marker struct that is =#[allow(dead_code)]=, which is a
   per-entity cost for a static image and hides the intent.

   - Files: =src/visuals.rs=, =src/map.rs=
   - Acceptance:
     - [ ] The 768 tile entities are replaced by a single tiled render pass, or =MapTile= becomes a used marker and its =#[allow(dead_code)]= is removed.
     - [ ] The board renders identically to today.
     - [ ] Startup entity count drops by the tile count, measured and recorded.

* Severity A — a third one, the stall that was the presentation layer

** DONE [#A] Find out why a windowed peer stops updating                     :@net:test:
   :PROPERTIES:
   :ID:         audit-031
   :EFFORT:     1d
   :END:

   *D33. Found by measuring, not by reading: a peer that renders stops running its
   `Update` schedule after one to two seconds on this machine, and the host does it
   too.* It is the reason three of the verifications above are weaker than they
   look, and it is filed at =[#A]= because an unexplained stall in the main loop is
   the kind of thing that turns out to be a deadlock.

   What was measured, on a windowed client dialling a headless server, with
   =RUST_LOG=info,greentd=debug= and =stdbuf -oL= so the log could not be lost to a
   block buffer:

   #+BEGIN_EXAMPLE
   10:56:46 INFO greentd::ui::hud: PROBE frame=60  elapsed=1.1s
   10:56:47 INFO greentd::ui::hud: PROBE frame=120 elapsed=2.1s
   # (nothing further for the remaining 23 seconds)
   #+END_EXAMPLE

   and the same run's kernel counters, from =/proc/net/snmp=:

   #+BEGIN_EXAMPLE
   before: In=1732594 RcvbufErr=11206
   after:  In=1741394 RcvbufErr=11812     # +606 packets dropped for want of a reader
   #+END_EXAMPLE

   That is the whole explanation of the "flaky" remote delivery: while the loop is
   alive the notice arrives; when it stalls the socket buffer fills and the kernel
   drops it. A windowed =host= stalled too, once, before it ever logged wave 1.

   Hypotheses, none confirmed at the time: a =PresentMode::Fifo= present that
   blocks because the window is not on screen in this session; an event-loop wait
   that never wakes; a system of ours that blocks on something.

   - Files: =src/main.rs=, =ci/windowed-client-liveness.sh= (new)
   - Acceptance:
     - [x] Whether the stall is in winit/wgpu presentation or in this crate's systems is determined and written here.
     - [x] A windowed peer keeps updating for at least 60 seconds in a detached session, byte-for-byte as it does under a visible window, or the environment limitation is documented as unfixable with the evidence that says so.
     - [x] If it is ours, it stops stalling; if it is not, =audit-005= and =audit-003='s remote boxes can be ticked by a scripted run rather than a transcript.
     - [x] A test or a script exists that would have caught it: the natural one is the two-process harness of [[file:12-testing.org][12-testing.org]] =test-008=, which is also where =audit-030='s regression belongs.

   *The first hypothesis was the right one, and the answer is `present`.* Made
   deterministic instead of intermittent by hiding the window on demand -- one
   line, `swaymsg '[title="Green TD - client"] move scratchpad'`, while the client
   runs -- the stall reproduces on every attempt, and a backtrace from the blocked
   process names it exactly. Condensed to the frames that matter, main thread then
   render thread:

   #+BEGIN_EXAMPLE
   (main thread, blocked)
   #0  syscall ()
   #1  <std::sys::sync::condvar::futex::Condvar>::wait ()
   #7  parking::Inner::park (timeout=...)
   #8  ...::block_on (future=...)                    bevy_tasks/task_pool.rs:413
   #13 bevy_render::pipelined_rendering::renderer_extract (world=...)
   #16 bevy_render::pipelined_rendering::renderer_extract (world=...)
   #20 bevy_app::sub_app::SubApp::extract (self=..., world=...)
   #21 bevy_app::sub_app::SubApps::update (self=...)
   #22 bevy_winit::state::WinitAppRunnerState::run_app_update (self=...)   state.rs:772
   #23 bevy_winit::state::WinitAppRunnerState::redraw_requested (self=...) state.rs:620
   #24 bevy_winit::state::about_to_wait (self=...)                         state.rs:470

   (render thread, the thing the main thread is waiting for)
   #4  wl_display_poll ()                            libwayland-client.so
   #5  wl_display_dispatch_queue_timeout ()          libwayland-client.so
   #6  wl_display_dispatch_queue ()                  libwayland-client.so
   #7  wsi_wl_swapchain_queue_present ()             libvulkan_intel.so
   #8  wsi_common_queue_present ()                   libvulkan_intel.so
   #9  anv_QueuePresentKHR ()                        libvulkan_intel.so
   #10 ash::...::khr::swapchain::Device::queue_present (self=..., queue=..., ...)
   #17 wgpu::api::surface_texture::SurfaceTexture::present (self=...)
   #19 bevy_render::view::window::ExtractedWindow::present (self=...)
   #20 bevy_render::renderer::render_system (world=..., windows=...)   renderer/mod.rs:106
   #+END_EXAMPLE

   Read bottom-up on each. The event loop is *running*: =about_to_wait= called
   =run_app_update=. It is the update that stops, parked in =bevy_render='s
   =extract=, waiting on the render thread -- which is stuck in Mesa's Vulkan
   Wayland WSI, where =vkQueuePresentKHR= is sitting in =wl_display_dispatch_queue=
   waiting for the compositor to release a buffer, and a window that is not being
   presented never gets one. So the cause is the presentation layer, Mesa plus the
   compositor, and not one of this crate's systems: box one's answer, and it also
   rules out the "event loop never wakes" reading of the symptom, which is why
   =WinitSettings= makes no difference to it (tested: identical stall with a
   timer-driven =Reactive= update mode).

   *Fixed on our side by not presenting with FIFO.* =PresentMode::AutoNoVsync=
   picks a present mode that does not wait on a frame callback, so the render
   thread never parks and the update loop keeps running whether or not the window
   is on screen. Measured, with the window hidden for the middle of the run:

   #+BEGIN_EXAMPLE
   $ ci/windowed-client-liveness.sh                    # window left alone
   liveness: OK (4 waves delivered, each within 4s)
   $ LIVENESS_HIDE=1 ci/windowed-client-liveness.sh     # hidden for the middle third
   liveness: OK (4 waves delivered, each within 4s)
   #+END_EXAMPLE

   and the same script against a build without the present-mode change, as the
   negative control:

   #+BEGIN_EXAMPLE
   $ LIVENESS_HIDE=1 ci/windowed-client-liveness.sh
   liveness: FAIL: wave 2 notice was 8.1s late (tolerance 4)
   #+END_EXAMPLE

   The cost is vsync, and it is not free: with the blocking present gone, nothing
   else caps the frame rate, and the same change with Bevy's default free-running
   focused mode renders at about 330 fps. So =WinitSettings= is set to a
   timer-driven 60 Hz -- a frame *cap*, not the fix -- and the rate was measured
   back at 60 fps. The two changes are one fix and belong together; =main.rs=
   documents the pair at =win_settings=.

   *What this unblocks.* Remote delivery is no longer a transcript. A headless
   server plus a windowed client now delivers a notice for every wave, in order,
   within seconds, for the whole run -- the instrument =audit-005= and =audit-003=
   were owed, now a script (=ci/windowed-client-liveness.sh=) rather than a shell
   history. It also gives =audit-005='s third box a repeatable remote delivery to
   stand on, instead of the single host observation and traced send it had.

   Not done here, and said so rather than implied: the script is deliberately not
   wired into CI, because it needs a display *and* a Vulkan driver and a hosted
   runner has neither, so the job would always be skipped; the in-process harness
   that *would* be CI-able is =test-008='s, and it is still unwritten. The frame
   cap stays a constant because a display rate is not a balance number.

* Severity C — polish

** TODO [#C] Merge CreepVis and CreepHp into one replicated component                 :@net:
   :PROPERTIES:
   :ID:         audit-024
   :EFFORT:     2h
   :BLOCKED-BY: net-001
   :END:

   D12: =CreepVis= and =CreepHp= (=src/game.rs=) are registered independently in
   =build_protocol= (=src/protocol.rs=), so each creep can produce two replicated
   component updates per tick instead of one.

   - Files: =src/game.rs=, =src/protocol.rs=, =src/server.rs=, =src/visuals.rs=
   - Acceptance:
     - [ ] A single =CreepState= component carries position and health and is the only creep component on the wire, or a measured justification (component updates per creep per tick and bytes/s at 150 creeps and 30 Hz) is recorded for keeping the split.
     - [ ] =track_creeps= still re-renders a creep exactly once per changed tick.
     - [ ] =sync_creeps= writes one component per creep.
     - [ ] The protocol version hash (found-004) reflects the component-set change.

** TODO [#C] Replace the per-tower, per-creep scan with a spatial index               :@sim:
   :PROPERTIES:
   :ID:         audit-025
   :EFFORT:     1w
   :END:

   D16: target acquisition is O(towers x creeps) every tick and the splash pass
   repeats it. At the current caps (700 towers, 150 creeps) that is about 105k
   distance tests per tick, roughly 115k once the splash loop is counted, each
   calling =creep.pos(&self.path)= (a binary search over the path).

   - Files: =src/sim.rs=
   - Acceptance:
     - [ ] The body states the current complexity and the worst case (O(towers x creeps) plus a second O(towers x creeps) splash pass; about 105k-115k distance tests per tick at 700 towers and 150 creeps).
     - [ ] Targeting uses a uniform-grid index keyed by cell, cell size at least the maximum tower range, so a query touches only nearby creeps.
     - [ ] Behaviour is unchanged: the same creep is targeted and splash hits the same set as today, ties included.
     - [ ] Sim test (D17): for a fixed board the index-backed target and splash set are identical to the scan's.
     - [ ] A benchmark records at least an order of magnitude improvement at the worst case, and the number is written here.

** TODO [#C] Decide and document inside-ring versus outside-ring colouring            :@map:
   :PROPERTIES:
   :ID:         audit-026
   :EFFORT:     2h
   :END:

   D26: =setup_camera_and_map= (=src/visuals.rs=) colours a cell by =is_buildable=
   alone, so inside the ring and outside the ring look identical and nothing says
   whether that is intended.

   - Files: =src/map.rs=, =src/visuals.rs=
   - Acceptance:
     - [ ] Inside-ring and outside-ring ground are either rendered differently, or the decision to treat them identically is written as a comment in =src/map.rs= and cited here.
     - [ ] The board still renders and the path remains visually distinct from buildable ground.

** TODO [#C] Stop walking every path segment in distance_to                          :@map:
   :PROPERTIES:
   :ID:         audit-027
   :EFFORT:     1w
   :END:

   D28: =Path::distance_to= (=src/map.rs=) walks all four segments per call and
   =is_buildable= is called per click on the server. Cheap for a four-corner ring,
   wrong shape for a map with many waypoints.

   - Files: =src/map.rs=
   - Acceptance:
     - [ ] =distance_to= (or a cell-local test) no longer walks every segment for a grid query, and returns the same answer as today.
     - [ ] A benchmark confirms the improvement is not bought with a changed answer.
     - [ ] =Sim::is_buildable= still rejects a cell within =0.95 * TILE= of the path.

** DONE [#C] Mark the local player, tracked in found-010                              :@ui:
   :PROPERTIES:
   :ID:         audit-028
   :EFFORT:     2h
   :BLOCKED-BY: found-010
   :END:

   D30: =update_hud= (=src/client.rs=) infers "me" from the single =PlayerView= it
   happens to hold (=me.single()=), which is a coincidence and breaks in host mode.
   The fix is the =LocalPlayer= marker in =found-010=; this task tracks the defect in
   the audit index and records the client-side acceptance.

   - Files: =src/client.rs=, =src/game.rs=
   - Acceptance:
     - [x] =update_hud= reads gold and kills from a =LocalPlayer= query, never from =single()= over all =PlayerView=s (found-010).
     - [x] The player count is read from =MatchView=.
     - [x] The triage table lists D30 against =found-010=, not a second marker defined here.

   Closed by =found-010=, which is where the work was done; D30 is =DONE=.

* Sequencing

  Work top to bottom. The order is chosen so that each phase is safe to start on the
  code the previous phase left behind, and so that authority holes close before any
  new feature widens them.

  1. *Foundations of the audit.* =audit-010= (D17, the headless harness) and
     =audit-001= (D25, the =Reject= reasons) come first. Every later sim task adds a
     test, and the authority tasks need a reason to report. Neither changes behaviour
     on its own.
  2. *The [#A] authority cluster.* =audit-002= (D1/D2), =audit-003= (D3), =audit-004=
     (D4), =audit-005= (D5), =audit-006= (D6), =audit-007= (D7), =audit-008= (D8).
     These stop a client from draining or destroying another player's board, freezing
     the HUD, spamming waves or commands, or minting a zero-gold player. Do these
     before any feature work.
  3. *Identity, once =net-001= lands.* =audit-009= (D9) turns the socket address into
     a session identity; it is the prerequisite for reconnect and for the leaver rule
     to be trustworthy.
  4. *The wire-format batch, done together behind =net-001=.* =audit-009= (D9),
     =audit-011= (D18's balance hash), =audit-019= (D22) and =audit-020= (D23) all
     change or depend on the format, so doing them in one pass is cheaper than three;
     =audit-024= (D12, the component merge) joins this batch because it changes the
     replicated component set. None may start before =net-001= is =DONE=.
  5. *The design decision.* =audit-012= (D19) stays =WAITING= until
     =decide-default-lineage= is signed off. It blocks nothing else in this file, but
     it gates =sim-005=, and the lose condition cannot be finalised until it resolves.
  6. *The [#B] fidelity cluster.* =audit-013= (D10), =audit-014= (D11), =audit-015=
     (D13), =audit-016= (D14), =audit-017= (D15), =audit-018= (D20/D21), =audit-021=
     (D24), =audit-022= (D27), =audit-023= (D29). =audit-017= and =audit-018= are
     =DONE=; the rest are open and unblocked, except =audit-014=, whose sim half
     cannot be finished without =waves-004='s creep catalogue.
  7. *The [#C] polish.* =audit-024= through =audit-028=.

  Steps 1 and 2 are complete, and so are =audit-017=, =audit-028=, =audit-029=,
  =audit-030=, =audit-019=, =audit-011=, =audit-031= and =audit-018=. Three of
  them were found rather than planned, all by running the binary rather than
  reading it: =audit-029= (D31) because building CI meant running a server by
  hand, =audit-030= (D32) because running a server by hand *beside a client* is
  the only way to see a message that never leaves the process, and D34 because
  =audit-031= needed a script and the scripts it learned from had a namespace bug
  in them. That is four defects now -- D31, D32, D33 and D34 -- that no test in
  this repository could have caught, and it says the same thing each time: every
  task in this file was written from reading =src/=, and reading =src/= is what
  missed them.

  =audit-011= closed with a deliberate non-answer worth recording, because the
  same choice will come up again. Its body conceded that the remaining literals
  were =towers-002='s to absorb "if the tower rewrite slips", which reads as
  permission to leave them. The rewrite has not started, so exercising that
  permission would have meant D18 -- an =[#A]= -- staying open until M2 for no
  reason: three floats and a refund fraction are one `#[serde(default)]` field
  each on tables that already exist, and =towers-002= will delete them along with
  everything around them. The general rule this settles: *a task that names a
  literal in its body is not discharged by a later task that happens to remove
  the same line.* A deferral is only honest when the waiting task is the thing
  that makes the work possible; "somebody else will rewrite this file" is not.

  What it cost to close was small, and the shape is reusable: three fields on
  =TowerDef= (=damage_per_level=, =range_per_level=, =slow_duration=) and one on
  =MatchRules= (=sell_refund_percent=), each with a serde default so an existing
  table still loads, each range-checked in =validate= and canonicalised in
  =normalise= so it enters the hash the handshake already exchanges. The refund
  is an *integer* percent on purpose: =0.7= as an =f32= truncates 80 x 0.7 to 55
  and would have made a two-peer disagreement out of a rounding mode.

  =net-001= has landed, and it was the head of the remaining block: =audit-009=,
  =audit-011='s hash half, =audit-019=, =audit-020= and =audit-024= were all
  waiting on it, and =audit-019= closed with it because the refusal *is* the
  handshake's other outcome. It cost more than a version check, and the two
  things it cost are worth carrying forward:

  - a refusal that removes =Connected= before the send path runs is a refusal
    that is never written -- the same shape as =audit-030= (D32), and the second
    time in this project that a message has existed and not reached the wire;
  - a one-shot handshake can simply be lost, because lightyear's UDP server
    drops the payload of every packet that arrives while it is still spawning
    the peer, and the first datagram is the one it spawns on. A reliable channel
    cannot recover from that, because there is nobody yet to acknowledge it.
    Both were found by starting two processes and reading their logs; neither is
    visible to any test that exists today.

  =net-002= has landed too, and =audit-009= closed with it: the socket address is
  no longer a player. It also turned up a transport fact the plan did not
  anticipate -- *raw UDP never reports a dead peer*, measured at over twelve
  seconds and unnoticed by lightyear -- so the "refuse a second connection"
  reading of =net-002='s third box was impossible to honour without also making a
  reconnect impossible, and the documented rule is takeover instead. See
  =net-002= for the measurement; the session token that would let a returning
  client prove itself, and so narrow the takeover, is =net-003='s.

  What is left, and in what order:

  =audit-031= and =audit-018= have since closed too, and they closed in that
  order for a reason worth keeping: =audit-031= was the last =[#A]= and the thing
  that made several other verifications weaker than they read, and =audit-018= was
  the first task in this file that could be taken *after* it because it needed a
  windowed client to be worth re-running. What is left sorts into the startable
  and the waiting:

  *Startable now, in this order:*

  1. The =[#B]= fidelity cluster and the =[#C]= polish in steps 6 and 7 --
     =audit-013=, =audit-021=, =audit-022=, =audit-023=, =audit-024=, =audit-025=,
     =audit-026=, =audit-027=. None is blocked by anything outside this file, and
     the two that touch the map (=audit-026=, =audit-027=) are cheap because
     =03-map.org= has not yet replaced the geometry they read.
  2. =found-011= (D34), the =ci/= scripts' namespace bug, which is in
     [[file:00-foundation.org][00-foundation.org]] and not here. Two hours, no
     blockers, and it is currently step 3's procedure with a landmine under it:
     the smoke script a developer reaches for when the pipeline is red fails
     confusingly if they set any of its documented knobs.

  *Waiting on something, and on what:*

  3. =net-006='s last box, and with it =net-001='s two-process refusal and
     =audit-030='s regression. All three want the =App= harness of
     [[file:12-testing.org][12-testing.org]] =test-008=, which is blocked by
     =net-004= (the validation choke point). So the front of the netcode file is
     behind =net-004=, not behind anything this machine lacks.
  4. =decide-default-lineage=, which is a decision and not work and is what
     =sim-005= is waiting on. =audit-012= cannot move until it is signed off.
  5. =audit-020= (D23) is the last member of the wire-format batch that is work
     rather than a component-set change, and its acceptance box needs a second
     machine with a non-local host on it -- the same limitation =found-002='s last
     box has.

  =audit-031= (D33) is closed, so there is no =[#A]= left in this file. The
  stall that made three remote-delivery boxes transcripts rather than scripts was
  the presentation layer's, and =ci/windowed-client-liveness.sh= is the script
  that now holds them.

  =audit-018= (D20, D21) closed with it, and it cost less than its body expected
  because the two halves of "the match is over" turned out to be one request
  rather than two features. Three decisions from it are worth carrying forward,
  because each is a place a later task could re-open the same question:

  - *A reset is a request, not a button.* =ResetMatch= is a message; the timer and
    the control that fire it are =lobby-010='s and =ui-015='s. The temptation to
    put a key handler on it here would have made the server's restart semantics a
    client's business.
  - *A rematch keeps its players and reseeds its generator.* The player records
    survive and stay connected -- a rematch is the same people, and re-deriving
    identity would mean re-running the handshake (D9) -- but the RNG is rebuilt
    from the match seed, so a rematch on the same tables is the same match. The
    test asserts that by comparing a reset sim against a brand-new one, creep for
    creep.
  - *The mirrors were left alone, and that is the point.* =sync_creeps= and
    =sync_towers= already reap a mirror whose sim counterpart is gone, so an
    emptied sim empties the board by itself; a second reaper in the reset path
    would have been one more thing to keep in step. =lobby-010='s old acceptance
    box asked for the opposite (despawn and respawn every =PlayerView= too) and
    has been corrected to match.

  One addition to step 4, the wire-format batch, now that D32 has been seen: it
  should include a smoke-run of a *remote* client for every change to
  =src/net/server.rs=. =audit-030= is a two-word mistake that no test in this
  repository can catch, and the only instrument that sees it is
  =RUST_LOG=lightyear_debug::message=trace= on a server and a client at once. That
  is a procedure, not a task, and it belongs in the same list as "run all three
  modes by hand" in the =found-008= upgrade path. =audit-031= has since given that
  procedure a script to lean on -- =ci/windowed-client-liveness.sh= runs the
  remote pair and fails when a notice does not arrive -- so the trace is now for
  diagnosing a failure the script reports, rather than the only way to notice one.
